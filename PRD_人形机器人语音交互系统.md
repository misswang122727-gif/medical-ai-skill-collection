# 产品需求文档（PRD）
## 墨甲机器人语音交互系统

**版本**：v1.0  
**作者**：王珊珊  
**创建日期**：2026-05-27  
**状态**：草稿

---

## 1. 项目概述

### 1.1 项目背景

墨甲机器人作为面向家庭陪伴、商业服务与工业协作的新一代具身智能平台，正走向真实复杂的人居环境。与传统语音助手不同，墨甲机器人需要在自身运动过程中持续感知和理解用户语音：电机与关节运动产生的结构噪声叠加环境噪声，使信噪比（SNR）常态化低于5dB；多人并存的真实场景要求系统能够精确定位并锁定目标说话人；同时语音交互必须与运动控制、任务执行实时协同，现有单一ASR方案无法满足上述组合挑战。

当前墨甲机器人语音交互存在三大核心痛点：**模块割裂**（声学前端、ASR、TTS各自独立集成，链路时延叠加超标）、**噪声适配不足**（未针对墨甲结构噪声特性定制AEC和NS模型）、**大模型浅接入**（LLM以问答插件形式接入，未深度耦合机器人动作执行与多轮任务规划）。为此，需要建设一套专为墨甲机器人设计的全链路语音交互系统，贯通纯声学前端→多模态前端→唤醒→识别→中枢系统→TTS→大模型功能七大模块，实现端到端低延迟、高准确率、强兜底的闭环交互，成为墨甲机器人的"耳朵+大脑+嘴巴"。

### 1.2 项目目标

| 目标编号 | 目标描述 | 量化指标 | 完成时限 |
|----------|----------|----------|----------|
| G-01 | 降低端到端语音交互延迟 | 从用户发出指令到机器人开始响应（首字节TTS输出）≤800ms（安静环境）/ ≤1200ms（噪声环境SNR≥5dB） | 2026-Q3 |
| G-02 | 提升唤醒准确性 | 唤醒率（WRR）≥97%（1m范围内），误唤醒率（FAR）≤1次/8h待机 | 2026-Q3 |
| G-03 | 提升语音识别准确率 | 字错误率（CER）≤5%（普通话，安静环境）/ ≤10%（噪声环境SNR≥5dB） | 2026-Q4 |
| G-04 | 提升多模态感知能力 | 支持声学+视觉联合定向，说话人定位误差≤15°（180°正面范围内） | 2026-Q4 |
| G-05 | 实现大模型深度集成 | 复杂多轮指令理解正确率≥85%，支持工具调用（运动/感知/任务API） | 2026-Q4 |

### 1.3 范围

**包含模块：**
- 纯声学前端信号处理（回声消除、噪声抑制、增益控制、波束成形）
- 多模态前端信号处理（声学+视觉联合说话人定向与跟踪）
- 唤醒模块（关键词检测，低功耗边缘推理）
- 识别模块（ASR全链路，含声学模型+语言模型+领域热词）
- 中枢系统（NLU意图理解、多轮对话、指令路由、降级策略）
- TTS模块（实时语音合成，流式输出，拟人音色）
- 大模型功能（LLM接入、RAG知识增强、Function Call机器人动作调用）

**不包含模块：**
- 机器人运动控制系统（步态、平衡、抓取）
- 视觉感知系统本体（非联合语音定向用途的视觉算法）
- 硬件麦克风阵列选型与电路设计
- 机器人操作系统（ROS/ROS2）底层驱动
- 多语言（非中文/英文）支持（一期不包含）

### 1.4 术语与缩略语

| 缩略语 / 术语 | 全称 / 释义 |
|--------------|------------|
| AEC | Acoustic Echo Cancellation，声学回声消除 |
| NS | Noise Suppression，噪声抑制 |
| AGC | Automatic Gain Control，自动增益控制 |
| WRR | Wake-up Recognition Rate，唤醒率 |
| FAR | False Acceptance Rate，误唤醒率 |
| ASR | Automatic Speech Recognition，自动语音识别 |
| CER | Character Error Rate，字错误率 |
| AM | Acoustic Model，声学模型 |
| LM | Language Model，语言模型 |
| NLU | Natural Language Understanding，自然语言理解 |
| TTS | Text-to-Speech，语音合成 |
| LLM | Large Language Model，大语言模型 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| SNR | Signal-to-Noise Ratio，信噪比 |
| MOS | Mean Opinion Score，主观音质评分（TTS质量评估） |
| TTFT | Time To First Token，首字节输出时间 |
| 中枢系统 | 语音交互的决策与调度核心，负责意图理解、多轮管理、指令路由 |
| 结构噪声 | 机器人自身运动（电机、关节）产生的机械振动噪声 |

---

## 2. 角色与职责

### 2.1 用户角色

| 角色 | 描述 | 主要诉求 |
|------|------|----------|
| 普通用户 | 与机器人日常语音交互的人（家庭成员、商业场所顾客） | 唤醒灵敏、理解准确、响应自然、延迟低 |
| 专业操作员 | 部署现场的技术人员，负责机器人配置与调试 | 支持热词定制、噪声环境调参、日志可查 |
| 运维人员 | 远程监控机器人语音服务健康状态 | 关键指标可观测，异常可告警，故障快速定位 |
| 算法工程师 | 负责各模块模型训练与优化 | 模块接口标准、数据管道清晰、指标可复现 |
| 测试工程师 | 执行全链路语音交互测试 | 测试用例完备，自动化覆盖率高，边界场景可验收 |

### 2.2 责任方

| 责任域 | 负责方 | 关键交付 |
|--------|--------|----------|
| 产品设计 | AI 产品经理 | PRD、验收标准、用户场景 |
| 声学前端算法 | 音频算法团队 | AEC/NS/AGC/波束成形模型 |
| 唤醒算法 | 端侧算法团队 | 唤醒引擎、低功耗部署方案 |
| ASR算法 | 语音识别团队 | 声学模型、语言模型、热词 |
| NLU / 中枢 | 对话系统团队 | 意图识别、槽位填充、多轮管理 |
| TTS算法 | 语音合成团队 | 合成引擎、音色定制 |
| 大模型接入 | LLM工程团队 | 模型接入、工具调用、上下文管理 |
| 系统集成 | 机器人软件团队 | 全链路联调、ROS接口对接 |
| 测试验收 | 测试工程师 | 测试用例、压测报告、上线验收 |

---

## 3. 业务场景

### 3.1 主场景流程：用户与墨甲机器人完整语音交互

```
步骤1  待机状态
       机器人低功耗运行，唤醒模块持续监听，其余模块休眠

步骤2  唤醒触发
       用户说出唤醒词（如"小度你好"）
       → 纯声学前端实时处理麦克风输入（AEC+NS+AGC）
       → 唤醒模块检测到关键词，置信度 ≥ 阈值
       → 触发全链路激活，机器人发出唤醒确认音/动作

步骤3  指令录入
       用户说出指令（如"帮我把桌上的水杯拿过来"）
       → 多模态前端联合声学+视觉定向，锁定说话人
       → ASR识别模块实时转写为文本

步骤4  意图理解与决策
       → 中枢系统解析意图（动作指令：拿取 + 目标：水杯 + 位置：桌上）
       → 判断是否需要调用大模型（简单指令走规则，复杂推理走LLM）
       → 大模型 Function Call 下发运动控制指令

步骤5  执行与反馈
       → 机器人执行运动，TTS合成"好的，我去帮您拿"并输出
       → 任务完成后TTS播报结果

步骤6  多轮对话（可选）
       → 用户追问，中枢系统维护对话上下文，继续步骤3循环

步骤7  会话结束
       → 超时无输入（默认30s）自动回到待机状态
```

### 3.2 异常场景

| 异常类型 | 触发条件 | 处理策略 |
|----------|----------|----------|
| 结构噪声干扰 | 机器人运动时SNR<5dB | AEC+NS联合处理；运动状态下提高唤醒阈值，降低误唤醒；运动结束后0.5s内不进行识别 |
| 多人同时说话 | 检测到2个以上说话人 | 多模态前端优先锁定距离最近或目光注视机器人的用户；无法区分时TTS播报"请一位用户发言" |
| 远场指令（>3m） | 麦克风输入SNR<0dB | 波束成形增强；识别置信度<阈值时追问确认，不直接执行动作指令 |
| ASR识别失败 | CER>30% 或超时 | 降级回复"抱歉，我没听清，请再说一次"，最多重试2次；连续失败后引导用户使用APP控制 |
| LLM超时 | 推理时间>3s | 中枢系统降级到规则引擎处理；同时异步继续LLM推理，结果用于后续轮次 |
| TTS合成失败 | 合成模块异常 | 降级为预录制音频片段播报，覆盖常用回复语；同时告警通知运维 |
| 网络中断 | 云端API不可达 | 本地端侧LLM降级（能力受限，仅支持基础指令）；本地ASR模型接管；TTS使用本地引擎 |
| 唤醒词被电视/音箱触发 | 检测到非人类说话人特征 | 多模态验证：声源方向无人脸则拒绝唤醒；FAR超标时动态提高唤醒阈值 |

---

## 4. 功能需求（FR）

### FR-001：纯声学前端信号处理

**输入：** 麦克风阵列原始音频流（PCM，16kHz/48kHz，多通道）  
**处理逻辑：**
1. AEC（回声消除）：消除机器人TTS播放时产生的自身回声，参考信号来自TTS输出通道
2. NS（噪声抑制）：抑制结构噪声（电机/关节）及环境噪声，保留人声频段
3. AGC（自动增益控制）：将人声音量归一化到目标电平（-26dBFS±3dB）
4. 波束成形：基于多模态定向结果，增强目标方向语音，抑制旁瓣噪声

**输出：** 单通道增强音频流（PCM，16kHz），配置SNR估计值供下游模块判断置信度  
**优先级：** P0

---

### FR-002：多模态前端信号处理

**输入：** 声学前端增强音频 + 视觉模块人脸/骨骼位置坐标（实时帧）  
**处理逻辑：**
1. 声源定向（DOA）：基于麦克风阵列相位差估计声源方向角（水平±180°，垂直±30°）
2. 声视融合：将声学DOA与视觉人脸坐标对齐，确定唯一目标说话人
3. 多说话人仲裁：多人同时发言时，按距离优先、注视优先规则选定目标
4. 持续跟踪：目标说话人移动时，实时更新方向并调整波束成形参数

**输出：** 目标说话人方向角（°）+ 置信度（0-1）+ 目标切换事件通知  
**优先级：** P0

---

### FR-003：唤醒模块

**输入：** 声学前端增强音频（持续监听流）  
**处理逻辑：**
1. 关键词检测：端侧模型（<10MB）持续检测预设唤醒词，支持定制1-3个唤醒词
2. 置信度过滤：置信度 ≥ 动态阈值（默认0.85，噪声环境自动提升）时触发唤醒
3. 防误唤醒：唤醒前30s内已唤醒过则进入保护期，不重复唤醒
4. 低功耗策略：唤醒模型独立运行于低功耗处理器，其余模块唤醒后才完全激活

**输出：** 唤醒事件（含唤醒词文本、置信度、时间戳）/ 静默（无事件）  
**优先级：** P0

---

### FR-004：识别模块（ASR）

**输入：** 唤醒后的目标说话人音频流（16kHz，单通道）  
**处理逻辑：**
1. 端点检测（VAD）：自动检测语音起止点，支持停顿容忍（≤800ms停顿不断句）
2. 声学模型推理：将音频帧转为音素概率序列
3. 语言模型解码：结合领域语言模型、热词列表，生成最优文本假设
4. 流式输出：逐词/逐字实时输出中间结果，最终结果在句末确认
5. 热词增强：支持运行时动态注入热词（机器人名字、场景词汇），权重可调

**输出：** 转写文本（含实时中间结果和最终确认结果）+ 置信度 + 时间戳  
**优先级：** P0

---

### FR-005：中枢系统

**输入：** ASR最终转写文本 + 当前对话上下文（多轮历史）+ 机器人状态  
**处理逻辑：**
1. 意图识别（Intent Classification）：判断指令类别（运动控制/信息查询/任务执行/闲聊/无效指令）
2. 槽位填充（Slot Filling）：提取指令中的实体（动作、对象、位置、时间）
3. 多轮对话管理：维护会话状态机，支持追问澄清、上下文指代消解
4. 指令路由：
   - 简单确定性指令 → 规则引擎直接下发
   - 复杂/模糊指令 → 调用大模型推理
   - 超出能力边界 → 礼貌拒绝并提示
5. 降级策略：大模型不可用时，规则引擎兜底处理高频指令

**输出：** 结构化指令（JSON：intent、slots、action、parameters）+ 回复文本（供TTS）  
**优先级：** P0

---

### FR-006：TTS模块

**输入：** 中枢系统输出的回复文本  
**处理逻辑：**
1. 文本预处理：数字/符号/缩写规范化，中英混读处理
2. 韵律预测：基于语义分析预测停顿、重音、语速、语调
3. 声学合成：将韵律特征转为音频波形，支持流式逐句输出
4. 情感适配：根据场景标签（询问/确认/警告/闲聊）自动调整音色情感
5. 发音人定制：支持配置机器人专属音色（预录制+迁移学习）

**输出：** 音频流（PCM，24kHz）+ 字级时间戳（供嘴型同步）  
**优先级：** P0

---

### FR-007：大模型功能

**输入：** 中枢系统路由的复杂指令 + 对话历史 + 机器人状态上下文  
**处理逻辑：**
1. LLM推理：基于上下文生成自然语言回复或结构化决策
2. RAG知识增强：检索机器人知识库（产品手册、场景规则、环境地图），补充LLM上下文
3. Function Call：将LLM输出映射到机器人能力接口（moveArm、navigate、graspObject等）
4. 上下文管理：滑动窗口维护对话历史（≤4000 tokens），超长时自动摘要压缩
5. 成本控制：简单指令不调用LLM；批量非实时任务使用异步API

**输出：** 自然语言回复文本 / 结构化Function Call调用 / 澄清追问  
**优先级：** P0

---

## 5. 非功能需求（NFR）

### 5.1 性能

| 指标 | 要求 | 测试场景 |
|------|------|----------|
| 端到端交互延迟（唤醒→TTS首帧） | ≤800ms（安静）/ ≤1200ms（SNR≥5dB） | 1m距离，普通话 |
| 唤醒率（WRR） | ≥97% | 1m，0°正面，安静环境 |
| 误唤醒率（FAR） | ≤1次/8h | 典型家庭/商业环境持续播放 |
| 字错误率（CER） | ≤5%（安静）/ ≤10%（SNR≥5dB） | 标准测试集（500句） |
| TTS自然度（MOS） | ≥4.0 | 盲测主观评分，5分制 |
| TTS首字节延迟（TTFT） | ≤200ms（**不含云端网络RTT；网络RTT单独预算 ≤150ms**） | 句长≤20字，云端合成 |
| 多模态定向误差 | ≤15° | 180°正面范围，1-3m |
| 本地降级ASR CER | ≤15% | 高频指令集（200句） |

### 5.2 可靠性

| 指标 | 要求 |
|------|------|
| 语音服务可用性 | ≥99.5%（本地链路）/ ≥99.0%（含云端LLM） |
| 云端中断降级能力 | 网络中断后≤500ms切换本地模式，本地模式覆盖≥80%高频指令 |
| MTBF | ≥720h（连续工作） |
| MTTR | ≤5min（软件级重启恢复） |
| 数据持久化 | 对话日志实时写入，机器人重启后可恢复最近会话上下文 |

### 5.3 安全

| 维度 | 要求 |
|------|------|
| 语音数据传输 | HTTPS/WSS（TLS 1.3+），音频流端到端加密 |
| 本地存储 | 用户语音数据本地加密（AES-256），默认不上传原始音频 |
| 隐私合规 | 非唤醒状态下不录制、不存储语音；唤醒词检测在端侧完成，不经云端 |
| 危险指令防护 | 中枢系统内置安全策略，拒绝执行危险动作指令（高速运动/向人挥击等） |
| 防声纹欺骗 | 支持活体检测（可选），区分真人语音与录音播放 |

### 5.4 兼容性

| 维度 | 要求 |
|------|------|
| 机器人平台 | 兼容主流墨甲机器人平台（ROS2接口，支持ARM/x86计算单元） |
| 麦克风阵列 | 支持4-8阵元线性/圆形麦克风阵列，采样率16kHz/48kHz |
| 操作系统 | Ubuntu 20.04/22.04，实时内核（RT-Linux） |
| LLM后端 | 支持云端API（OpenAI兼容接口）+ 本地部署（≥7B量化模型） |
| 语言 | 普通话（一期），英语/粤语（二期规划） |

### 5.5 成本

| 项目 | 约束 |
|------|------|
| 端侧推理功耗 | 唤醒模块独立运行 ≤0.5W，全链路激活 ≤15W |
| 云端API月度成本 | ≤¥20,000/百台机器人（含LLM+云端ASR） |
| 本地存储占用 | 语音相关模型+数据 ≤8GB |
| 延迟预算分配（规则路径） | 声学前端≤30ms / 多模态前端≤50ms / 唤醒≤50ms / ASR出文延迟≤300ms / 中枢≤100ms / TTS首帧≤200ms（不含云端网络RTT，单独预算≤150ms） |
| 延迟预算分配（LLM路径） | 以上各项不变，新增云端LLM推理≤2000ms（P95，含网络RTT）；首字反馈"让我想一下"应在≤800ms内由规则引擎先行返回 |

---

## 6. 数据需求

### 6.1 输入数据

| 数据类型 | 来源 | 格式 | 关键字段 | 更新频率 |
|----------|------|------|----------|----------|
| 麦克风音频流 | 机器人麦克风阵列 | PCM，16/48kHz，多通道 | 通道数、采样率、时间戳 | 实时 |
| 视觉定位数据 | 机器人视觉模块 | JSON，人脸/骨骼坐标 | face_id、x/y/z坐标、置信度 | 30fps |
| 机器人状态 | 机器人控制系统 | JSON | 运动状态、关节状态、任务状态 | 10Hz |
| 热词列表 | 运营平台配置 | JSON | word、weight、phoneme | 按需推送 |
| 知识库 | 运营平台内容管理 | Markdown/JSON | title、content、category | 每日同步 |

### 6.2 输出数据

| 数据类型 | 用途 | 存储周期 | 脱敏规则 |
|----------|------|----------|----------|
| 对话文本日志 | 质量分析、模型迭代 | 30天（本地）/ 90天（云端） | 不上传原始音频；文本中手机/身份证等敏感信息脱敏 |
| 性能指标日志 | 运维监控 | 90天 | 无需脱敏 |
| 异常事件日志 | 故障分析 | 180天 | 无需脱敏 |
| TTS音频输出 | 实时播放 | 不持久化 | — |

### 6.3 数据质量

| 维度 | 要求 |
|------|------|
| 时序一致性 | 声学与视觉数据时间戳对齐误差 ≤20ms |
| ASR文本完整性 | 每条指令必须包含转写文本、置信度、时间戳，缺字段不下发中枢 |
| 热词覆盖率 | 运营配置的热词24h内同步到所有在线机器人，成功率 ≥99% |

---

## 7. 接口需求

### 7.1 内部接口（模块间）

| 接口 | 上游 | 下游 | 关键参数 | 时延SLA |
|------|------|------|----------|---------|
| 增强音频流 | 声学前端 | 唤醒/ASR | PCM，16kHz，SNR估计值 | ≤30ms |
| 说话人定向结果 | 多模态前端 | 声学前端/ASR | 方向角、置信度 | ≤50ms |
| 唤醒事件 | 唤醒模块 | 中枢系统 | 唤醒词、置信度、时间戳 | ≤50ms |
| ASR结果 | 识别模块 | 中枢系统 | 转写文本、置信度、是否最终结果 | ≤300ms |
| 结构化指令 | 中枢系统 | 大模型/运动控制 | intent、slots、action | ≤100ms |
| 回复文本 | 中枢系统/大模型 | TTS | 文本、情感标签 | ≤100ms |
| TTS音频流 | TTS模块 | 扬声器/嘴型同步 | PCM，24kHz，字级时间戳 | 首帧≤200ms（不含云端网络RTT） |

### 7.2 外部接口

| 接口 | 类型 | 说明 | SLA |
|------|------|------|-----|
| 云端LLM API | HTTP/WebSocket | OpenAI兼容接口，Function Call | P99 ≤2s |
| 云端ASR API | WebSocket流式 | 备用/增强识别 | P99 ≤500ms |
| 机器人运动控制API | ROS2 Service/Action | 执行动作指令 | ≤100ms |
| 知识库同步服务 | HTTPS | 增量同步热词和知识库 | ≤24h延迟 |
| 运维监控平台 | HTTPS | 性能指标上报、告警 | 准实时（≤5s） |

### 7.3 错误处理

| 场景 | 策略 |
|------|------|
| 云端LLM超时（>3s） | 降级规则引擎；超时事件上报监控；用户侧TTS播报"让我想一下" |
| ASR识别置信度<0.6 | 中枢系统发起澄清追问，不下发执行指令；连续2次追问失败则结束会话 |
| 模块内部异常（崩溃） | 自动重启对应模块（≤3s）；重启失败则全链路降级并告警 |
| 运动控制接口超时 | 取消当前动作，TTS播报"操作未完成"，记录故障日志 |
| 熔断策略 | 任意云端接口连续失败5次，触发熔断，切换本地模式，60s后尝试恢复 |

---

## 8. 风险与约束

### 8.1 技术风险

| 风险 | 可能性 | 影响 | 应对方案 |
|------|--------|------|----------|
| 结构噪声与AEC适配不足 | 高 | 高 | 在真实机器人平台上采集噪声数据专项训练AEC模型；运动状态强制提高识别门槛 |
| 端到端延迟超标 | 中 | 高 | 模块间异步流水线优化；TTS流式输出；在ASR出中间结果时即触发LLM预推理 |
| 本地LLM能力不足 | 中 | 中 | 本地模型仅覆盖高频指令（Top 100）；超出能力引导用户联网或简化指令 |
| 唤醒误触发 | 中 | 中 | 多模态验证（声源方向+人脸检测）联合判定；A/B测试唤醒阈值 |
| LLM Function Call映射错误 | 低 | 高 | 危险动作白名单强制校验；LLM输出经安全层过滤后才下发执行 |

### 8.2 资源约束

| 约束类型 | 上限 |
|----------|------|
| 项目周期 | 一期MVP上线：2026-Q4 |
| 端侧计算资源 | ARM Cortex-A78 8核 + NPU（≤8 TOPS）|
| 内存限制 | 全链路运行内存 ≤6GB |
| 研发人力 | 算法3人（前端/ASR/TTS各1）、中枢系统2人、大模型接入1人、集成测试1人 |

### 8.3 依赖

| 依赖项 | 依赖类型 | 风险 |
|--------|----------|------|
| 机器人硬件平台（麦克风阵列+扬声器） | 硬件 | 阵列布局影响声学前端效果，需联合调优 |
| 视觉感知模块（人脸/骨骼检测） | 内部系统 | 接口稳定性、帧率是否满足多模态融合要求 |
| 云端LLM服务 | 外部服务 | 接口变更、价格波动、网络稳定性 |
| ROS2机器人控制接口 | 内部系统 | 接口版本管理、动作原子性 |

---

## 9. 验收标准

### 9.1 功能验收

| FR编号 | 测试用例 | 通过条件 |
|--------|----------|----------|
| FR-001 | 播放TTS同时采集音频，测量AEC消除效果 | 回声抑制≥30dB，人声无明显失真（MOS≥3.8） |
| FR-002 | 声学+视觉联合定向，测试人移动场景 | 方向角误差≤15°，跟踪切换延迟≤200ms |
| FR-003 | 100次唤醒词测试（1m，安静环境） | WRR≥97%，FAR测试（8h）≤1次 |
| FR-004 | 500句标准测试集识别 | 安静CER≤5%，SNR≥5dB场景CER≤10% |
| FR-005 | 50条指令覆盖5类意图（运动/查询/任务/闲聊/无效） | 意图识别准确率≥90%，槽位准确率≥88% |
| FR-006 | 100条文本TTS合成，盲测主观评分 | MOS≥4.0，首字节延迟≤200ms |
| FR-007 | 30条复杂多轮指令+Function Call调用 | 正确路由率≥85%，危险指令拦截率100% |

### 9.2 性能验收

| 场景 | 测试参数 | 工具 | 通过条件 |
|------|----------|------|----------|
| 端到端延迟 | 100次唤醒→TTS首帧，安静环境 | 自动化时序测量脚本 | P95≤800ms，P99≤1000ms |
| 并发压力 | 模拟5台机器人同时请求云端LLM | 压测脚本 | 成功率≥99%，P99延迟≤3s |
| 本地降级 | 断网状态下执行50条高频指令 | 手动测试 | 完成率≥80%，切换延迟≤500ms |
| 长期稳定性 | 72h连续运行，每小时100次交互 | 自动化测试框架 | 无崩溃，性能指标无衰减≥10% |

### 9.3 上线验收

| 环节 | 标准 |
|------|------|
| 内测阶段 | 5台机器人内部实验室测试，持续2周，记录全量交互日志 |
| 小批量灰度 | 20台机器人部署真实场景（家庭/商业）测试2周，核心指标达标后扩量 |
| 全量上线条件 | 唤醒率≥95%，端到端P95延迟≤900ms，本地降级成功率≥75%（均满足方可全量） |
| 回滚条件 | 任意核心指标连续48h低于阈值80%，或出现安全类Bug（危险指令未被拦截），立即回滚 |
| 上线后监控 | 全量上线后持续监控4周，每日输出语音交互质量日报（唤醒率/CER/延迟P95/异常占比） |

---

*文档结束*
