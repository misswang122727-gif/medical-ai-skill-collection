# 智能座舱语音交互系统 SOR 需求规格说明书

**版本**：v1.1（0→1 精简版，对齐海外公版端到端响应基线）  
**作者**：王珊珊  
**日期**：2026-05-27  
**适用系统**：中高端智能座舱语音交互全链路 · 纯语音 · 第一版

> **修订摘要（v1.1 vs v1.0）**
> - 加入**海外公版测试工况**（§2）与**业务域端到端响应表**（§4），数值对齐海外基线 + 国内极氪/领克/一汽参考
> - 删除整章硬件规格（移至专项采购规格）、L0-L4 噪声分级、LLM 通用基准 7 项基准、多音区分人详细规格——这些非 0→1 必需，二期再补
> - 各模块表压缩到 3-5 行核心指标，全文 10 章 → 6 章

---

## 1. 基本信息

| 字段 | 内容 |
|------|------|
| 项目名称 | 智能座舱语音交互系统 |
| 适用产品 | 中高端乘用车智能座舱（一期） |
| 算力基线 | 高通骁龙 8295（兼容龙鹰一号 / 8155） |
| 麦克风阵列 | 6 麦分布式（车顶前 2 + A 柱 2 + 后排顶 2），支持 4 音区 |
| 文档状态 | 草稿，待评审 |

**包含范围（M1-M7 + 端到端集成）**
M1 声学前端 / M2 多音区识别与说话人定向 / M3 唤醒 / M4 ASR / M5 中枢系统 / M6 TTS / M7 大模型功能

**不包含范围（供应商不得纳入投标与报价）**
- 视觉语言理解 VLM（图像描述 / VQA / OCR / 视觉空间推理 / POPE）
- 嘴型同步 / 虚拟形象
- 整车 OTA 与车云后端服务
- 车端运动控制 / 车机操作系统底层驱动
- 一期 3 语种以外的多语言

---

## 2. 测试工况统一约定（对齐海外公版）

> 本 SOR 所有指标均在以下工况下测试，禁止供应商更换更宽松条件复测。

| 项目 | 设定值 |
|------|--------|
| 车速 | 0 km/h（怠速） |
| 主/副驾车窗 | 关 |
| 空调 | 2 档 |
| MIC 处环境噪声压级 | 45-50 dBA |
| MIC 处语音声压级 | 65-75 dBA |
| 语速 | 220-300 词/min |
| 语言 | 各语种通用 |
| 内存占用率 | 60%-80% |
| **VAD pause time** | 海外 500 ms / 国内 300 ms（国内端到端正常比海外多 200 ms） |

---

## 3. 模块需求（M1-M7）

> 所有指标按 **shall（资格底线）/ should（加分位）** 双层量化

### 3.1 M1 声学前端

| 指标 | shall | should |
|------|-------|--------|
| AEC 回声抑制量（ERLE） | ≥ 25 dB | ≥ 30 dB |
| NS 信噪比改善 | ≥ 10 dB | ≥ 12 dB |
| 处理延迟 | ≤ 30 ms | ≤ 20 ms |

### 3.2 M2 多音区识别与说话人定向

| 指标 | shall | should |
|------|-------|--------|
| 物理音区数 | 4（主驾 / 副驾 / 后排左 / 后排右） | 4 |
| 相邻音区串扰抑制 | ≥ 15 dB | ≥ 20 dB |
| 单人音区识别率 | ≥ 92% | ≥ 96% |
| 处理延迟 | ≤ 50 ms | ≤ 40 ms |

### 3.3 M3 唤醒

| 指标 | shall | should |
|------|-------|--------|
| 唤醒率 WRR（标准工况，§2） | ≥ 95% | ≥ 97% |
| 误唤醒率 FAR | ≤ 0.5 次/8h | ≤ 0.2 次/8h |
| **唤醒响应时间**（含播放器延迟 ≤ 50 ms） | **≤ 350 ms** | ≤ 300 ms |
| 端侧模型大小 | ≤ 5 MB | ≤ 2 MB |
| 待机功耗 | ≤ 0.5 W | ≤ 0.3 W |

### 3.4 M4 ASR（识别）

| 指标 | shall | should |
|------|-------|--------|
| 字错误率 CER（在线，标准工况） | ≤ 5% | ≤ 3% |
| 字错误率 CER（离线，车控 200 词集） | ≤ 6% | ≤ 4% |
| **流式首字上屏延迟（在线）** | **≤ 900 ms** | ≤ 700 ms（对齐一汽） |
| **流式首字上屏延迟（离线）** | **≤ 850 ms** | ≤ 650 ms（对齐极氪/领克/一汽） |
| 热词召回率（车型/地名/曲名歌手） | ≥ 95% | ≥ 98% |

### 3.5 M5 中枢系统

| 指标 | shall | should |
|------|-------|--------|
| 意图分类准确率（6 类：车控/导航/多媒体/查询/闲聊/无效） | ≥ 92% | ≥ 95% |
| 规则路由准确率（200 条无歧义指令） | ≥ 98% | ≥ 99% |
| **危险指令拦截率** | **100%** | 100%（不分层） |
| 规则路径处理延迟 | ≤ 100 ms | ≤ 60 ms |

### 3.6 M6 TTS

| 指标 | shall | should |
|------|-------|--------|
| 自然度 MOS（5 分制盲测） | ≥ 4.1 | ≥ 4.3 |
| 云端首音频帧延迟（不含网络 RTT） | ≤ 200 ms | ≤ 150 ms |
| 离线 TTS 首帧延迟 | ≤ 300 ms | ≤ 200 ms |
| 多音字/专有名词正确率 | ≥ 95% | ≥ 98% |

### 3.7 M7 大模型功能

> 仅含纯文本 LLM 推理、RAG、Function Call、上下文管理；**不含 VLM**

| 指标 | shall | should |
|------|-------|--------|
| Function Call 动作映射准确率 | ≥ 85% | ≥ 92% |
| **危险指令拦截率** | **100%** | 100%（不分层） |
| 云端 LLM 推理延迟 P95（含 RTT ≤ 150 ms） | ≤ 2000 ms | ≤ 1500 ms |
| 离线 7B 量化模型 Top 100 高频指令覆盖率 | ≥ 80% | ≥ 90% |

---

## 4. 端到端响应指标（对齐海外公版 + 国内对标）

> 测试工况：§2 标准工况  
> 计时口径：用户说完最后一字 → 车机首字 TTS 输出 / 车控指令在 CAN 总线下发

### 4.1 在线响应（联网状态）

| 业务域 | shall | should | 国内对标参考 | 备注 |
|--------|-------|--------|-------------|------|
| 唤醒响应 | ≤ 350 ms | ≤ 300 ms | — | 含播放器延迟 ≤ 50 ms |
| **车控**（车辆/系统控制/空调/播控/地图操作） | ≤ 1500 ms | ≤ 1300 ms | 极氪 1500 / 领克 1300 | 基准业务域 |
| **电话** | ≤ 1700 ms | ≤ 1500 ms | 极氪/领克 1500 | 比车控多 nativeAPI + 1 次云交互（+200 ms） |
| **多媒体** | ≤ 2200 ms | ≤ 2200 ms | 极氪 2200 / 领克 2300 | 比车控多信源耗时 ≤ 700 ms |
| **地图（导航）** | ≤ 2500 ms | ≤ 2100 ms | 极氪 2100 / 领克 2300 | 比车控多信源 ≤ 800 ms + 200 ms |
| **天气** | ≤ 2500 ms | ≤ 1500 ms | 极氪 1500 / 领克 2000 | 比车控多信源 ≤ 1000 ms |
| **ASR 首字上屏** | ≤ 900 ms | ≤ 700 ms | 一汽 700 | 比离线多网络传输耗时 |

### 4.2 离线响应（断网兜底）

| 业务域 | shall | should | 国内对标参考 | 备注 |
|--------|-------|--------|-------------|------|
| **车控** | ≤ 1400 ms | ≤ 1200 ms | — | 离线基准 |
| **电话** | ≤ 1500 ms | ≤ 1300 ms | — | 比车控多 nativeAPI + 离线技能（+100 ms） |
| **ASR 首字上屏** | ≤ 850 ms | ≤ 650 ms | 极氪 700 / 领克 650 / 一汽 650 | 小语种首字延迟比中文多 200 ms |
| 多媒体 / 地图 / 天气 | 一期**不强制**离线 | — | — | 二期补 |

### 4.3 端到端任务成功率

| 任务类型 | shall | should | 测试方法 |
|----------|-------|--------|----------|
| 车控指令端到端成功率 | ≥ 92% | ≥ 96% | ASR 正确 ∧ NLU 正确 ∧ FC 正确 ∧ CAN 下发成功 |
| 复杂多轮（≥ 3 轮）任务成功率 | ≥ 80% | ≥ 88% | 100 组多轮测试集 |

### 4.4 网络切换无感

| 指标 | shall | should |
|------|-------|--------|
| 在线 → 离线切换时间 | ≤ 500 ms | ≤ 200 ms |
| 切换瞬间指令丢失 | 0 | 0 |
| 离线 → 在线恢复 | ≤ 1 s | ≤ 500 ms |

---

## 5. 异常兜底

| 异常类型 | 触发条件 | 处理策略 |
|----------|----------|----------|
| 误唤醒（广告/电台触发） | 多模态验证失败：DMS 无注视 + 声源非合理方位 | 拒绝唤醒，FAR 计数器+1 |
| ASR 置信度低 | 置信度 < 0.6 | 中枢追问澄清；连续 2 次失败结束会话 |
| LLM 超时 | 推理 > 3 s | 降级规则引擎；TTS 先播「让我想一下」首字反馈 |
| 网络中断 | 云 API 不可达 | 本地 ASR + 本地 7B LLM + 本地 TTS 接管，切换 ≤ 500 ms |
| 危险/敏感指令 | 行驶中开车门 / 极端操作 | 100% 拦截，TTS 提示「行驶中不可执行」 |
| 模块崩溃 | 进程异常 | ≤ 3 s 自动重启；HMI 提示「语音服务恢复中」 |

---

## 6. 验收标准

### 6.1 模块验收
M1-M7 每条 shall 指标对应一条测试用例，含：**测试方法 / 通过条件 / 测试工具**。模块指标全部通过 shall = 模块达标。

### 6.2 端到端验收

| 验收维度 | 通过条件 |
|----------|----------|
| §4.1 在线 7 个业务域延迟 | 100% 满足 shall（5 个业务域 × P95，外加唤醒与首字上屏） |
| §4.2 离线 3 个业务域延迟 | 100% 满足 shall |
| §4.3 端到端任务成功率 | 100% 满足 shall |
| §4.4 网络切换 | 100% 满足 shall |

### 6.3 上线灰度

| 阶段 | 标准 |
|------|------|
| 内测 | 5 台样车实验室 4 周 |
| 小批量灰度 | 20 台真实路况 4 周 |
| 全量条件 | WRR ≥ 95% ∧ 车控在线端到端 P95 ≤ 1500 ms ∧ 任务成功率 ≥ 92% |
| 回滚条件 | 任意核心指标连续 72 h 低于 shall 80% / 出现安全类 Bug（危险指令未拦截） |
| 上线后监控 | 持续 8 周日报：WRR / CER / 车控端到端 P95 / 任务成功率 / 误唤醒数 |

---

*SOR v1.1（精简 0→1 版） | 与 PRD_智能座舱语音交互_v1.0 配套使用 | 二期补：硬件规格 / LLM 通用基准 / 高速/极端噪声场景 / 多音区分人详细规格*
