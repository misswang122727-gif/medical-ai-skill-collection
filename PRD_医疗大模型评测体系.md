# 产品需求文档（PRD）
## 医疗大模型评测体系（Xpert Medical Health Benchmark）

**版本**：v1.0  
**作者**：王珊珊  
**创建日期**：2026-05-27  
**状态**：草稿

---

## 1. 项目概述

### 1.1 项目背景

当前医疗健康领域的大语言模型（LLM）能力评估体系严重缺失：市面上通用 LLM 评测基准（如 MMLU、C-Eval）无法有效衡量模型在临床场景下的实际表现，临床医生与 AI 产品团队之间缺乏共同语言，导致模型上线后频繁出现"评测好看、落地差"的问题。与此同时，医疗 AI 评测题目的生产依赖人工专家，流程分散、质量参差不齐、周期长且难以复用，产品迭代成本极高。

为解决上述痛点，ByteDance Xpert 平台需要一套**从 0 到 1 的医疗大模型评测基准**，覆盖 20 个以上临床场景，实现出题—质检—验收全流程标准化，并通过 MCP（Model Control Protocol）服务器与 AI 工具链深度集成，最终为医疗 AI 产品决策提供可量化、可复现的评测依据。

### 1.2 项目目标

| 目标编号 | 目标描述 | 量化指标 | 完成时限 |
|----------|----------|----------|----------|
| G-01 | 建立覆盖临床主要场景的评测题库 | 上线后 3 个月内完成 ≥20 个临床场景、≥2000 道题目的首批入库 | 2026-Q3 |
| G-02 | 提升评测题目生产效率 | 单条题目从创作到入库周期由原 ≥3 天缩短至 ≤8 小时 | 2026-Q4 |
| G-03 | 保障评测题目质量 | 专家二审通过率 ≥85%，错误率（医学事实、标注）≤3% | 持续 |
| G-04 | 实现端到端流程自动化 | 出题、质检、报告生成三个核心环节自动化率 ≥70% | 2026-Q4 |
| G-05 | 支撑医疗 AI 产品迭代决策 | 每次模型版本发布前 2 天内输出评测报告 | 持续 |

### 1.3 范围

**包含模块：**
- 评测题库管理（题目创作、存储、版本控制）
- 临床场景分类体系（≥20 个场景）
- 多级质检流程（AI 自检 → 专家初审 → 交叉验收）
- 模型评测执行引擎（批量推理、指标计算）
- 评测报告生成与可视化
- MCP 服务器集成接口
- Health Program Expert 技能文件（含 prompt、few-shot、参考知识库）

**不包含模块：**
- 医疗 AI 模型本体的训练与微调
- 面向 C 端用户的医疗咨询产品
- 临床数据的采集与脱敏（依赖外部数据团队提供）
- 多语言（非中文/英文）评测

### 1.4 术语与缩略语

| 缩略语 / 术语 | 全称 / 释义 |
|--------------|------------|
| LLM | Large Language Model，大语言模型 |
| Xpert | ByteDance 内部专家基准评测平台 |
| MCP | Model Control Protocol，模型控制协议（Claude 工具调用服务器） |
| FR | Functional Requirement，功能需求 |
| NFR | Non-Functional Requirement，非功能需求 |
| P0/P1/P2 | 需求优先级：P0 核心必做，P1 重要，P2 锦上添花 |
| 临床场景 | 医疗诊疗流程中具体的业务场景，如"药物相互作用查询"、"影像描述解读" |
| 质检 | 对评测题目的医学事实、标注准确性进行审核 |
| 验收 | 多方交叉确认题目符合入库标准的最终审核环节 |
| 评测报告 | 描述模型在各场景、各维度得分的结构化分析文档 |
| Skill 文件 | Claude Code 可调用的技能包，包含 prompt、few-shot、知识库引用 |

---

## 2. 角色与职责

### 2.1 用户角色

| 角色 | 描述 | 主要诉求 |
|------|------|----------|
| AI 产品经理 | 主要使用者，使用 Health Program Expert 技能主导评测体系落地 | 快速生成标准化评测题目，获取可决策的评测报告 |
| 医学领域专家 | 临床医师、药师等，提供题目审核与知识验证 | 高效完成质检任务，减少重复劳动 |
| 算法工程师 | 接入评测基准对模型进行对比测试 | API 稳定、指标清晰、结果可复现 |
| 测试工程师 | 执行评测流程、监控质量指标 | 自动化流程稳定，异常可追溯 |
| 运维人员 | 维护 MCP 服务器和评测系统运行 | 系统可观测、故障快速定位 |

### 2.2 责任方

| 责任域 | 负责方 | 关键交付 |
|--------|--------|----------|
| 产品设计 | AI 产品经理（王珊珊） | PRD、场景分类体系、验收标准 |
| Prompt 工程 | AI 产品经理 + 算法团队 | Skill 文件、few-shot 库、系统 prompt |
| 评测算法 | 算法工程师 | 评分模型、指标计算逻辑 |
| 后端开发 | 后端工程师 | MCP 服务器接口、题库 CRUD、报告生成 |
| 质量保障 | 测试工程师 + 医学专家 | 质检规则、验收报告 |
| 知识库维护 | 医学内容团队 | 参考知识库（检验指标、药品信息等）更新 |

---

## 3. 业务场景

### 3.1 主场景流程：0-to-1 评测体系搭建

```
步骤1  产品经理激活 Health Program Expert 技能
       → 调用 MCP 服务器，初始化评测项目

步骤2  输入临床场景列表（≥20 个）
       → 技能自动生成场景分类体系与出题模板

步骤3  批量生成评测题目（问题 + 选项 + 标准答案 + 解析）
       → AI 自检：格式合规性、选项排他性、答案唯一性

步骤4  医学专家初审
       → 标注审核意见（通过 / 修改 / 拒绝），返回修改建议

步骤5  交叉验收
       → 不同专家对高分歧题目进行二次确认

步骤6  题目入库
       → 分配场景标签、难度分级（初级/中级/高级）、知识点标签

步骤7  执行模型评测
       → 批量推理，计算准确率、覆盖率、场景得分

步骤8  生成评测报告
       → 可视化各场景得分、模型版本对比、问题分析

步骤9  决策与迭代
       → 产品/算法团队依据报告决定模型上线或继续优化
```

### 3.2 异常场景

| 异常类型 | 触发条件 | 处理策略 |
|----------|----------|----------|
| MCP 服务器离线 | 工具调用超时 >5s | 降级到离线模式，缓存最近一次 Skill 配置，告警通知运维 |
| 题目生成质量低 | AI 自检通过率 <60% | 自动切换 few-shot 策略，扩充示例库；连续 3 次失败时暂停并通知产品经理 |
| 专家审核超时 | 单题审核 >48h 无响应 | 自动发送催审通知，超 72h 升级至备用专家 |
| 指令冲突 | 用户同时下发"生成题目"与"清空题库" | 拒绝清空操作，提示用户确认，记录操作日志 |
| 系统过载 | 并发批量推理任务 >10 | 排队限流，优先处理 P0 级评测任务，返回预计等待时长 |
| 知识库缺失 | 题目涉及领域无对应参考知识库 | 自动标记题目为"待补充知识库"，允许专家手动填充来源引用 |

---

## 4. 功能需求（FR）

### FR-001：临床场景分类体系管理

**输入：** 产品经理输入业务关键词（如"心血管"、"儿科"、"药物咨询"）  
**处理逻辑：**
1. 调用场景分类 API，返回推荐场景列表及场景描述
2. 支持手动新增、编辑、停用场景
3. 场景间支持父子层级关系（最多 2 级）

**输出：** 结构化场景分类树（JSON 格式），含场景 ID、名称、描述、状态  
**优先级：** P0

---

### FR-002：评测题目自动生成

**输入：** 目标场景 ID、题目数量、难度分布（初/中/高比例）、题型（单选/多选/判断/填空）  
**处理逻辑：**
1. Health Program Expert 技能调用系统 prompt + 场景 few-shot 示例
2. 生成题目结构：题干 + 选项（A/B/C/D）+ 正确答案 + 解析 + 知识点标签
3. AI 自检执行格式、逻辑、答案唯一性校验
4. 自检通过率 <80% 时自动重新生成（最多重试 3 次）

**输出：** 题目草稿列表（含自检报告）  
**优先级：** P0

---

### FR-003：多级质检流程

**输入：** 待审题目列表、审核人员分配规则  
**处理逻辑：**
1. **一审（AI 自检）**：格式、答案唯一性、选项互斥性
2. **二审（专家初审）**：医学事实准确性、临床适用性、难度合理性
3. **三审（交叉验收）**：对二审分歧率 >20% 的题目安排第二专家复核
4. 审核状态流转：草稿 → 一审中 → 二审中 → 已入库 / 已拒绝 / 待修改

**输出：** 审核意见（通过/修改/拒绝）+ 修改建议文本 + 状态流转记录  
**优先级：** P0

---

### FR-004：题库管理

**输入：** 题目元数据（场景、难度、题型、知识点）  
**处理逻辑：**
1. CRUD 操作：创建、读取、更新、逻辑删除
2. 支持按场景、难度、知识点、审核状态多维度筛选
3. 版本控制：每次修改保留历史版本，支持回滚
4. 批量操作：批量导入（Excel/JSON）、批量导出、批量标签修改

**输出：** 题目详情页 + 操作日志  
**优先级：** P0

---

### FR-005：模型评测执行

**输入：** 模型接入配置（API endpoint、鉴权）、评测题库范围、评测参数（temperature=0, top_p=1）  
**处理逻辑：**
1. 批量推理：按题目顺序依次请求模型，记录原始回答
2. 答案解析：从模型输出中提取最终答案
3. 指标计算：准确率（Accuracy）、F1、场景覆盖率、失败率
4. 支持多模型横向对比（同一题库，不同模型版本）

**输出：** 原始推理结果 CSV + 指标汇总 JSON  
**优先级：** P0

---

### FR-006：评测报告自动生成

**输入：** 指标汇总 JSON + 模型版本信息 + 历史基线数据  
**处理逻辑：**
1. 生成各场景得分雷达图、模型版本趋势折线图
2. 自动识别得分下降 >5% 的场景，生成问题分析段落
3. 报告格式支持 Markdown / PDF / HTML 三种导出

**输出：** 评测报告文件（含图表）+ 摘要段落  
**优先级：** P1

---

### FR-007：MCP 服务器集成

**输入：** Claude Code 工具调用请求  
**处理逻辑：**
1. 暴露标准 MCP tool 接口：`generate_questions`、`run_evaluation`、`get_report`
2. 支持 Health Program Expert 技能文件的动态加载与更新
3. 请求鉴权：Bearer Token，有效期 24h

**输出：** 标准 JSON 响应（含 status、data、error_code）  
**优先级：** P0

---

### FR-008：知识库管理

**输入：** 医学参考文档（检验参考范围、药品说明书、诊疗指南等）  
**处理逻辑：**
1. 支持 Markdown / PDF 格式上传，自动解析并索引
2. 版本化管理，支持按文档类型、更新日期检索
3. 题目生成时自动关联相关知识库条目作为解析来源

**输出：** 知识库条目列表 + 引用链接  
**优先级：** P1

---

### FR-009：操作审计日志

**输入：** 所有写操作（题目增删改、模型评测触发、报告生成）  
**处理逻辑：** 记录操作人、时间戳、操作类型、操作对象 ID、操作前/后快照  
**输出：** 可查询的审计日志（保留 180 天）  
**优先级：** P1

---

## 5. 非功能需求（NFR）

### 5.1 性能

| 指标 | 要求 |
|------|------|
| 题目生成响应时间 | 单题生成 ≤30s，批量 100 题 ≤10min |
| 评测执行吞吐量 | 支持并发评测任务 ≥5 个 |
| 报告生成时间 | ≤2min（题库规模 ≤2000 题） |
| API 接口响应时间（P99） | ≤500ms（查询类），≤2s（写入类） |
| AI 自检准确率 | 格式校验准确率 ≥99%，医学事实检测召回率 ≥70% |

### 5.2 可靠性

| 指标 | 要求 |
|------|------|
| 系统可用性 | ≥99.5%（年度） |
| MTBF | ≥720h |
| MTTR | ≤2h |
| 数据持久化 | 题库数据实时备份，RPO ≤1h，RTO ≤4h |
| 降级策略 | MCP 服务不可用时，支持离线 Skill 文件本地加载，核心出题功能降级可用 |

### 5.3 安全

| 维度 | 要求 |
|------|------|
| 传输加密 | 所有 API 通信使用 HTTPS（TLS 1.2+） |
| 数据存储 | 题库数据静态加密（AES-256） |
| 权限控制 | RBAC 模型：产品经理、专家、工程师、只读访客四级权限 |
| 医疗隐私 | 题目内容不得包含真实患者信息；涉及案例使用匿名化处理 |
| 防注入 | 所有用户输入经过 Prompt Injection 检测，拒绝包含指令覆盖的恶意输入 |
| 审计合规 | 操作日志不可篡改，满足内部合规审计要求 |

### 5.4 兼容性

| 维度 | 要求 |
|------|------|
| Claude 模型版本 | 支持 Claude Haiku 4.5 / Sonnet 4.6 / Opus 4.7 |
| MCP 协议版本 | 兼容 MCP 1.x |
| 浏览器 | Chrome 120+、Edge 120+（报告可视化） |
| 导出格式 | Markdown、PDF、Excel（.xlsx）、JSON |
| 接入模型 | 支持 OpenAI 兼容接口（/v1/chat/completions）和自定义 HTTP 接口 |

### 5.5 成本

| 项目 | 约束 |
|------|------|
| 单题生成 Token 成本 | ≤$0.01（使用 Haiku 模型） |
| 月度算力预算 | ≤¥50,000（含评测执行、题目生成） |
| 存储成本 | 题库 + 知识库年存储 ≤100GB |

---

## 6. 数据需求

### 6.1 输入数据

| 数据类型 | 来源 | 格式 | 关键字段 | 更新频率 |
|----------|------|------|----------|----------|
| 临床场景定义 | 产品团队人工整理 | JSON | scene_id, name, description, parent_id | 按需 |
| Few-shot 示例 | 医学专家标注 | JSON | question, options, answer, explanation, scene | 双周 |
| 医学参考知识库 | 权威指南 / 药品说明 | Markdown/PDF | title, content, source, version, update_date | 月度 |
| 用户输入（题目生成指令） | AI 产品经理 | 自然语言 | scene_id, count, difficulty_dist, question_type | 实时 |
| 模型推理结果 | 被评测 LLM | JSON | question_id, model_answer, raw_output, latency | 评测时 |

### 6.2 输出数据

| 数据类型 | 用途 | 存储周期 | 脱敏规则 |
|----------|------|----------|----------|
| 评测题库 | 模型评测执行 | 永久（逻辑删除） | 题目内容不含真实患者信息 |
| 审核记录 | 质量追溯 | 2 年 | 无特殊脱敏要求 |
| 模型推理结果 | 评测报告依据 | 1 年 | 无 |
| 评测报告 | 产品决策 | 永久 | 无 |
| 操作审计日志 | 合规审计 | 180 天 | 无 |

### 6.3 数据质量

| 维度 | 要求 |
|------|------|
| 准确率 | 题目医学事实准确率（专家验收后）≥97% |
| 完整性 | 每道题必填字段（题干、选项、答案、解析、场景标签）完整率 100% |
| 一致性 | 同一知识点在不同题目中的表述和答案不冲突 |
| 去重率 | 题库内相似度 >85% 的题目自动告警，人工判断是否合并 |

---

## 7. 接口需求

### 7.1 内部接口

| 接口名称 | 调用方 | 被调用方 | 关键参数 | 返回码 |
|----------|--------|----------|----------|--------|
| `POST /questions/generate` | 前端/MCP | 题目生成引擎 | scene_id, count, difficulty, type | 200/400/503 |
| `POST /questions/{id}/review` | 质检模块 | 题库服务 | review_result, comment | 200/404 |
| `POST /evaluations/run` | 评测调度 | 推理引擎 | model_config, question_ids | 202/400 |
| `GET /evaluations/{id}/report` | 报告服务 | 评测结果存储 | evaluation_id | 200/404 |
| `GET /knowledge-base/search` | 题目生成引擎 | 知识库服务 | query, scene_id, top_k | 200/400 |

### 7.2 外部接口

| 接口 | 类型 | 说明 | SLA |
|------|------|------|-----|
| Claude API（Anthropic） | LLM 推理 | 题目生成、AI 自检 | 响应 P99 ≤10s |
| MCP Server | 工具调用协议 | Claude Code 技能调用入口 | 可用性 ≥99.9% |
| 被评测模型 API | HTTP/OpenAI 兼容 | 第三方或自研模型 | 依赖被测方 SLA |
| 内部 SSO | OAuth 2.0 | 用户身份认证 | 依赖平台 |

### 7.3 错误处理

| 场景 | 策略 |
|------|------|
| LLM API 超时（>30s） | 重试 2 次（指数退避 2s/4s），仍失败则返回 503 + 任务入队 |
| MCP 服务不可用 | 熔断：连续失败 5 次触发，熔断持续 60s，期间走降级路径 |
| 题目生成失败 | 记录失败原因，支持断点续生（按题目 ID 范围重试） |
| 知识库检索无结果 | 返回空列表 + 提示用户补充知识库，不阻断题目生成 |
| 模型推理乱码 / 无效输出 | 标记该题结果为 NULL，计入失败率，不影响其他题目计算 |
| 告警阈值 | 失败率 >10% 或响应时间 P99 >5s 时触发飞书 / 邮件告警 |

---

## 8. 风险与约束

### 8.1 技术风险

| 风险 | 可能性 | 影响 | 应对方案 |
|------|--------|------|----------|
| LLM 生成题目医学事实错误率超标 | 中 | 高 | 增加 few-shot 示例数量；强制双专家审核高难度题目 |
| 评测延迟超标（批量推理 >1h） | 低 | 中 | 异步任务队列；支持评测任务拆分并行 |
| 模型版本升级导致 prompt 失效 | 中 | 高 | Prompt 版本化管理，升级前执行回归测试（≥200 题） |
| MCP 协议变更 | 低 | 中 | 接口抽象层隔离，变更时只需更新适配层 |
| 医学专家资源不足导致质检瓶颈 | 高 | 中 | 提高 AI 自检能力覆盖率；建立外部专家备用池 |

### 8.2 资源约束

| 约束类型 | 上限 |
|----------|------|
| 项目预算 | ¥300,000（含算力、人力、知识库采购） |
| 时间 | 首版 MVP 上线：2026-Q3 末 |
| 技术人力 | 后端 2 人、算法 1 人、产品 1 人、测试 1 人 |
| 医学专家人力 | 兼职专家 5 名，每人每周可审核 ≤50 题 |

### 8.3 依赖

| 依赖项 | 依赖类型 | 风险 |
|--------|----------|------|
| Anthropic Claude API | 外部服务 | API 限流、价格变动 |
| MCP 服务器（Claude Code） | 平台依赖 | 协议升级、功能变更 |
| 内部 Xpert 平台 | 内部系统 | 接口变更、资源调度优先级 |
| 医学知识库授权 | 第三方内容 | 版权、更新及时性 |
| 被测模型 API | 算法团队 | 接口稳定性、版本管理 |

---

## 9. 验收标准

### 9.1 功能验收

| FR 编号 | 测试用例 | 通过条件 |
|---------|----------|----------|
| FR-001 | 新增、编辑、停用场景，父子层级正确显示 | 全部操作符合预期，层级最大 2 级 |
| FR-002 | 输入"内科/高难度/30 题"，触发生成 | 30 分钟内返回 30 道格式完整的题目，AI 自检通过率 ≥80% |
| FR-003 | 专家登录后完成 10 道题的审核并提交意见 | 审核意见保存成功，状态正确流转 |
| FR-004 | 批量导入 100 道题，导出后字段与原始一致 | 字段完整性 100%，版本历史可查 |
| FR-005 | 接入 GPT-4o，对 200 道题执行评测 | 结果 CSV 包含全部 200 道，指标计算误差 ≤0.1% |
| FR-006 | 评测完成后自动生成报告 | 报告含雷达图，得分下降场景有文字分析，导出 PDF 成功 |
| FR-007 | Claude Code 调用 `generate_questions` 工具 | 返回 HTTP 200，数据格式符合 MCP 规范 |
| FR-008 | 上传《中国高血压防治指南》PDF | 解析成功，题目生成时可引用对应知识点 |
| FR-009 | 执行题目删除操作，查询审计日志 | 日志包含操作人、时间戳、被删除题目 ID |

### 9.2 性能验收

| 场景 | 压测参数 | 工具 | 通过条件 |
|------|----------|------|----------|
| 批量题目生成 | 100 道/次，并发 3 个任务 | 自动化脚本 | 全部在 15min 内完成，失败率 <5% |
| 评测执行 | 2000 道题，1 个模型 | 评测调度脚本 | 完成时间 ≤120min，结果完整率 100% |
| API 查询接口 | 50 并发，持续 5min | Locust | P99 ≤500ms，错误率 <1% |
| 报告生成 | 2000 题评测结果 | 单元测试 | 生成时间 ≤2min |

### 9.3 上线验收

| 环节 | 标准 |
|------|------|
| 灰度发布 | 首周对 10% AI 产品经理用户开放；次周扩至 50%；第三周全量 |
| 观察周期 | 全量上线后观察 2 周，监控题目生成成功率、AI 自检通过率、专家审核响应时间 |
| 核心指标阈值 | 题目生成成功率 ≥95%；AI 自检通过率 ≥80%；专家 24h 审核完成率 ≥70% |
| 回滚条件 | 任意核心指标连续 2 天低于阈值的 80%，或出现医学事实重大错误（P0 级 Bug），立即回滚到上一版本 |
| 上线后首批验证 | 首版上线后 2 周内完成 3 个临床场景（心血管、糖尿病、药物咨询）的完整评测闭环 |

---

*文档结束*
