# 墨甲机器人语音交互系统 SOR 评审报告

**评审对象**：`skill/Voice_AI_Signal_Expert_SOR.md`（对应导出件 `Voice_AI_SOR_墨甲机器人.docx`，用户本地命名为「墨甲智创二代机器人人机交互系统 SOR_V2.0」）
**评审目标**：(1) 排查逻辑硬伤；(2) 核验指标参数是否过激进/属于"国际顶尖"范畴，是否能落到国内主流供应商可交付范围。
**评审结论**：**整体结构合规，但存在 6 处逻辑硬伤 / 跨章节不一致，以及 3 处指标偏激进、需向国内行业主流值回调**。已在同 PR 中给出落地修订（详见末节"已应用的修订"）。

---

## 一、逻辑硬伤（必须修复）

### 硬伤 1：波束成形指标与默认硬件不匹配
| 位置 | 内容 |
|------|------|
| §2.1 | 波束成形 主瓣增益 ≥ 6 dB（**4 阵元线阵**） |
| §3.2 默认硬件 | **6 阵元圆形阵列**，48 kHz |
| §PRD 5.4 兼容性 | 支持 4–8 阵元线性/圆形 |

测试条件用的阵列（4 元线阵）与默认部署阵列（6 元圆阵）不一致，供应商无法判断到底按哪个阵型交付。

### 硬伤 2：6 dB 主瓣增益 = 4 元阵列理论上限，工程上不可达
4 个全向阵元在远场弥散噪声下的最大理论阵列增益 = 10·log₁₀(N) = **6 dB**，这是物理上限。实际由于：失配、近场效应、噪声相关性、校准误差，工程值通常落在 **3–5 dB**。

写"≥ 6 dB（4 阵元线阵）"等于要求供应商交付理论极限，**国内任何主流声学厂商（科大讯飞 / 思必驰 / 声智 / 出门问问 / 云知声）都做不到**。

**建议**：
- 若坚持 4 元线阵 → 改为 **≥ 4 dB**
- 若要 ≥ 6 dB → 改为 **6 元圆阵**（与 §3.2 默认硬件一致），并将 4 元线阵作为"降级硬件"列入备注

### 硬伤 3：端到端时延预算只覆盖规则路径，LLM 路径缺位
| 位置 | 内容 |
|------|------|
| §6 C-08 文字描述 | 端到端总预算：安静 ≤ 800 ms，噪声 ≤ 1200 ms |
| §6 预算分配表 | 合计：安静 730 ms / 噪声 910 ms（**只含规则路径**） |
| §2.5 | LLM 路径总延迟 ≤ 3000 ms（P99） |
| §2.7 | 云端 LLM P95 ≤ 2000 ms / P99 ≤ 3000 ms |

如果指令走 LLM，端到端 = 30 + 50 + 50 + 300 + **2000~3000** + 200 = **2630~3630 ms**，**远超 800/1200 ms 上限**。表面上"满足预算"，实际上业务一旦走复杂指令就必然超标，供应商在验收时会与产品扯皮。

**建议**：把端到端预算拆成两条独立通道——
- **规则路径**：安静 ≤ 800 ms / 噪声 ≤ 1200 ms（现表合计 730 / 910，留有余量）
- **LLM 路径**：安静 ≤ 3500 ms（P95）/ 噪声 ≤ 4000 ms（P95），首字反馈（TTS 首帧）仍受 ≤ 1200 ms 约束（即"让我想一下"先播）

### 硬伤 4："首词输出延迟"名称与测量起点矛盾
| 位置 | 原文 |
|------|------|
| §2.4 | 流式识别 **首词**输出延迟 ≤ 300 ms（句长 5–20 字，**句末 VAD 触发后计时**） |

"首词"本意是从说话开始到 ASR 出第一字（即流式识别的首字延迟）；而当前测量起点是"句末 VAD 触发"，这是**尾包延迟 / 出文延迟**。名实不符，会导致供应商按字面意思（开始说话→首字）做，最后在验收时双方各执一词。

**建议**：改名为 **「出文延迟（VAD 句末触发 → ASR 最终结果输出）」**，或新增两个独立指标：
- **首字延迟（首字上屏）** ≤ 500 ms（说话开始计时）
- **尾包延迟（VAD 句末触发 → 最终结果）** ≤ 300 ms

### 硬伤 5：声视时间对齐 ≤ 20 ms 低于视觉单帧周期
| 位置 | 原文 |
|------|------|
| §2.2 | 声视时间对齐误差 ≤ 20 ms（视觉 30 fps，声学 16 kHz） |

30 fps 视觉单帧周期 = 33.3 ms。**纯软件时间戳同步**（基于系统时钟+帧 metadata）通常只能做到 40–60 ms；要做到 20 ms 必须有硬件级时间戳同步（PTP / IEEE 1588 / 触发线同步），机器人主板上不一定具备。

**建议**：二选一：
- 放宽到 **≤ 40 ms**（与一帧周期同量级，纯软件可达，国内行业主流）
- 保持 ≤ 20 ms，但**明确测试条件加注**"要求硬件 PTP 同步或共时钟域采集"

### 硬伤 6：唤醒/ASR 在噪声场景下的延迟在 §2 缺定义，但 §6 预算表有约束
| 位置 | 内容 |
|------|------|
| §6 预算表 | 唤醒：安静 ≤ 50 ms / **噪声 ≤ 80 ms**；ASR：安静 ≤ 300 ms / **噪声 ≤ 450 ms** |
| §2.3 | 仅给出"唤醒词检测延迟 ≤ 50 ms"（未说环境） |
| §2.4 | 仅给出"出文延迟 ≤ 300 ms"（未说环境） |

供应商按 §2 验收时只见安静值，按 §6 又被要求噪声值，**验收用例无法书写**。

**建议**：在 §2.3 / §2.4 各增一行噪声场景下的延迟指标，与 §6 预算表对齐。

---

## 二、跨文档一致性问题（PRD ↔ SOR）

### 不一致 1：TTS TTFT 是否含网络 RTT
| 位置 | 描述 |
|------|------|
| §SOR 2.6 | TTS TTFT ≤ 200 ms（**不含**网络 RTT） |
| §PRD 5.1 / 7.1 | TTS 首字节延迟 ≤ 200 ms（未说是否含 RTT） |

国内云厂商往返 RTT 典型 30–80 ms（同城）/ 50–150 ms（跨区）。如果 PRD 默认包含 RTT，云端 TTS 合成自身只剩 50–170 ms 预算，对小厂供应商不现实。

**建议**：PRD § 5.1 / § 7.1 显式标注"**不含云端网络 RTT；网络 RTT 单独预算 ≤ 150 ms**"，与 SOR 对齐。同时在 §6 预算表新增"云端网络 RTT ≤ 150 ms"一行。

### 不一致 2：规则引擎兜底覆盖率 80% 出现两次但含义不同
| 位置 | 描述 |
|------|------|
| §SOR 2.5 降级策略 | LLM **不可用**时规则兜底覆盖率 ≥ 80%（Top 100，断网） |
| §SOR 2.7 成本控制 | 简单指令 LLM 调用率 ≤ 20%（200 条明确简单指令，规则覆盖 80%） |

一个是"故障态"，一个是"正常稳态"，但叙述上容易让供应商当作同一指标。

**建议**：§2.7 那条改名 **"简单指令规则路由覆盖率 ≥ 80%"**，避免和降级率混淆。

---

## 三、指标偏激进 / 触碰国际顶尖（建议向国内行业主流回调）

| # | 指标 | 当前 | 国内主流（讯飞/思必驰/声智/出门问问/字节火山）| 建议 |
|---|------|------|----|------|
| 1 | 波束成形 主瓣增益（4 元线阵） | ≥ 6 dB | 3–5 dB | **≥ 4 dB**（或改 6 元圆阵 ≥ 6 dB） |
| 2 | 声视时间对齐误差 | ≤ 20 ms | 软件同步 40–60 ms | **≤ 40 ms**（或强制硬件 PTP） |
| 3 | TTS 多音字/专有名词正确率 | ≥ 98% | 95–97%（含定制词典） | **≥ 95%**（保留 98% 作为体验指标） |
| 4 | 单说话人正确锁定率 | ≥ 92%（1–3 m，180°） | 88–93% | 保留，但加测试条件"光照 ≥ 200 lux" |
| 5 | 2 人仲裁正确选择率 | ≥ 88% | 80–88% | 保留（紧但可达） |
| 6 | Function Call 动作映射正确率 | ≥ 85%（复杂多轮） | 75–85% | 保留（可达，与模型版本绑定） |
| 7 | RAG P@3 | ≥ 80% | 70–85% | 保留 |
| 8 | 危险指令拦截率 | 100% | 100%（白名单可保证） | ✓ 必须 100% |

其余指标（CER / WRR / FAR / MOS / TTFT / DOA / 待机功耗 / 阵列增益 / 嘴型同步）均处于**国内主流可达区间**，**无需回调**。

---

## 四、其他建议（非阻塞）

1. **§2.1 结构噪声测试条件**："麦克风距电机 30 cm"是实验室口径，但机器人实际安装距离由结构件决定（关节内电机往往 10–20 cm）。建议加注："30 cm 为评测基准距离；实机部署按实际安装距离复测，按 6 dB / 倍距 折算"。
2. **§2.3 唤醒模型 ≤ 10 MB**：国内端侧唤醒主流是 100 KB–2 MB；10 MB 显得过松，但**给供应商留余量是好事**，保留。
3. **§2.6 TTS 嘴型同步字级时间戳 ≤ 40 ms**：合理，对齐 25 fps 嘴型驱动正好一帧，无需修改。
4. **§6 C-08 时延预算建议增列"云端网络 RTT"独立行**，避免被各模块在自己延迟里分摊。
5. **§5.1 文档结构**："五章节固定"和 §4 Step 3–7 五项产出对应，但 §4 总共 8 步，文字上易让人误以为是 8 章节。建议在 §5.1 头部加一句："文档共 5 章节（对应工作流 Step 3–7），Step 1–2、8 为内部步骤不形成章节"。

---

## 五、已应用的修订（本 PR 改动）

| 序号 | 修改位置 | 修改内容 |
|------|----------|----------|
| F-1 | §2.1 波束成形 | 阵列基线从"4 阵元线阵 ≥ 6 dB"改为"**6 阵元圆阵 ≥ 6 dB / 4 阵元线阵 ≥ 4 dB**"，与 §3.2 默认硬件对齐 |
| F-2 | §2.1 结构噪声 | 加注"30 cm 为评测基准距离；实机按实际安装距离复测" |
| F-3 | §2.2 声视对齐 | 默认值改为 **≤ 40 ms**，并新增"硬件 PTP 同步时可达 ≤ 20 ms"作为体验指标 |
| F-4 | §2.3 唤醒 | 新增"噪声远场唤醒检测延迟 ≤ 80 ms"行 |
| F-5 | §2.4 ASR | 把"首词输出延迟"重命名为"**出文延迟（VAD 句末触发→最终结果）**"，并新增"噪声环境出文延迟 ≤ 450 ms" |
| F-6 | §2.5 / §2.7 | 把 §2.7 的"简单指令 LLM 调用率 ≤ 20%"明确为"简单指令规则路由覆盖率 ≥ 80%"，避免与 §2.5 降级率混淆 |
| F-7 | §2.6 TTS | 多音字/专有名词正确率核心指标 **≥ 95%**，新增体验指标 ≥ 98%；TTFT 加注"不含云端网络 RTT" |
| F-8 | §6 C-08 + 预算表 | 拆分**规则路径**与**LLM 路径**端到端预算，新增"云端网络 RTT ≤ 150 ms"独立行 |
| F-9 | PRD § 5.1 / § 7.1 | TTS 首字节延迟显式标注"不含云端网络 RTT" |

---

## 六、最终风险评估

| 风险 | 等级 | 处理 |
|------|------|------|
| 供应商交付不可达指标（4 元 6 dB / 20 ms 声视同步） | 高 → **已消除** | F-1、F-3 |
| 验收时两段路径口径不一致（规则 vs LLM） | 中 → **已消除** | F-8 |
| ASR 验收双方对延迟测量起点解读不一 | 中 → **已消除** | F-5 |
| 跨 PRD/SOR 网络 RTT 算法不一致 | 中 → **已消除** | F-7、F-9 |
| TTS 多音字 98% 在真实长尾词上可能达不到 | 低 → **已缓解** | F-7（拆核心/体验） |

修订后的 SOR **可作为对外发包基线**，供应商按表内指标可基于国内主流声学/语音/合成栈实现，不再触碰国际顶尖天花板。

---

*评审日期：2026-05-27 ｜ 评审版本：SOR v1.0（用户本地命名 v2.0） ｜ 修订版本：SOR v1.1*
