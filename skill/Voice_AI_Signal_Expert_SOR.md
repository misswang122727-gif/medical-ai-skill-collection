# 墨甲机器人语音交互系统 SOR 需求规格说明书

**版本**：v1.3（在 v1.2 基础上，参考海外公版端到端响应基线，加入统一测试工况与业务域端到端响应表，并按 0→1 删除非必要细节）  
**作者**：王珊珊  
**日期**：2026-05-27  
**适用系统**：墨甲机器人语音交互**纯语音链路**（M1 纯声学前端 / M2 多模态前端 / M3 唤醒 / M4 识别 / M5 中枢系统 / M6 TTS / M7 大模型功能）。**本期不含视觉语言理解（VLM）**。

> **v1.3 修订摘要**（在 v1.2 基础上）：
> 1. 新增 **§2.0 测试工况统一约定**（参考海外公版端到端响应指标 Excel）：噪声 45-50 dBA / 语音声压 65-75 dBA / 语速 220-300 词/min / 内存占用 60-80% / VAD pause time 国内 300 ms（端到端比海外多 200 ms）
> 2. 新增 **§6 端到端业务域响应表**：按机器人 6 类业务域（本体控制 / 信息查询 / 多媒体 / 任务执行 / 对话闲聊 / ASR 首字上屏）独立设定端到端响应上限，数值参考海外公版（车控 1500 / 电话 1700 / 地图 2500 / 多媒体 2200 / 天气 2500 / 首字上屏 900-850）映射至机器人场景
> 3. **§3.3 唤醒响应延迟** 对齐海外公版 ≤ 350 ms（含播放器延迟 ≤ 50 ms）
> 4. **§3.4 ASR 出文延迟** 改名为"首字上屏延迟"，对齐海外公版口径：在线 ≤ 900 ms / 离线 ≤ 850 ms
> 5. 删除 §2.7.2 LLM 通用能力基准 6 项（MMMLU/CMMLU/GSM8K/BBH/TruthfulQA/CoT），0→1 第一版不卡，二期补
> 6. 删除多模态前端 §2.2 "声视时间对齐 ≤ 20 ms 体验指标" 行（0→1 不必要）；唤醒 §2.3 多场景延迟、ASR §2.4 多噪声场景 CER 全部保留**安静工况一行 shall + 运动状态一行 shall**，删除其余细分档（机器人项目特性必须保留运动状态行）
> 7. 端到端延迟预算表（§6 原 A/B 双路径表）保留作为内部模块预算分配，业务域响应表作为对外承诺

---

> **v1.2 修订摘要**（在 v1.1 基础上）：
> 1. 显式声明 **VLM / 视觉语言理解（图像描述 / VQA / OCR / 视觉空间推理 / POPE 等）不在本期范围**；多模态前端的"视觉"仅指声视联合说话人定向，不做视觉内容理解；供应商不得将 VLM 能力纳入投标与报价
> 2. **§2.6 TTS MOS** 拆为双层指标：核心（shall）≥ 4.1，体验（should）≥ 4.3
> 3. **§2.7 大模型功能** 新增"LLM 通用能力基准（行业基线）"独立子表，全部按双层 shall/should 写明，对齐讯飞 Spark 4.0 Turbo / 思必驰对话 LLM 真实产品线能力：
>    - MMMLU shall ≥ 78% / should ≥ 82%
>    - **CMMLU**（替代 LogiQA，避开 GPT-4 都不到 60% 的硬伤）shall ≥ 70% / should ≥ 75%
>    - GSM8K shall ≥ 70% / should ≥ 80%（讯飞实测 89-94%，68% 反而显得 SOR 不专业）
>    - BBH shall ≥ 73% / should ≥ 80%
>    - TruthfulQA shall ≥ 0.52 / should ≥ 0.58
>    - CoT 完成率 shall ≥ 78% / should ≥ 85%
>    - Function Call shall ≥ 85% / should ≥ 92%（shall 与 v1.1 一致）
>
> v1.1 修订摘要（保留）：(a) 波束成形与默认硬件阵型对齐；(b) 端到端时延预算拆规则/LLM 双通道，新增云端 RTT 独立行；(c) ASR "首词" 重命名为"出文延迟"+ 噪声场景行；(d) 声视对齐放宽至 ≤ 40 ms，硬件 PTP 时 ≤ 20 ms；(e) TTS 多音字 shall ≥ 95% / should ≥ 98%；(f) 区分规则路由覆盖率（稳态）与规则兜底覆盖率（故障态）。详见配套 `SOR_Review_Report_墨甲v2.md`。

---

## 1. 技能基本信息（Meta Info）

| 字段 | 内容 |
|------|------|
| 技能名称 | 墨甲机器人语音链路需求撰写专家 |
| 技能版本 | v1.2（基于 v1.1 的供应商可达性校准） |
| 适用产品 | 墨甲人形机器人语音交互系统（全链路七大模块） |
| 优先级 | 6（高于通用分析类技能） |
| 文档状态 | 草稿，待评审 |

**核心适用场景**

| 场景类型 | 具体描述 |
|----------|----------|
| 核心场景 | 为七大语音模块逐一撰写标准化产品需求文档（PRD），含功能定义、指标、接口、异常、验收 |
| 次要场景 | 生成单模块性能指标清单、接口规范表、验收用例集 |

**禁用场景**

| 禁用项 | 原因 |
|--------|------|
| 异常根因诊断与排查 | 属于技术运维域，超出产品需求文档范围 |
| 竞品语音方案横向对比 | 数据来源难以核实，易引入未经验证数据 |
| 模型训练数据方案、算法优化建议 | 属于算法研发域 |
| 硬件麦克风阵列选型、电路与驱动层 | 属于硬件工程域 |
| 对已有PRD进行润色或局部修改 | 本技能定位为从零生成，不做增量修改 |
| **视觉语言理解（VLM）相关需求**（图像描述 / 视觉问答 VQA / OCR / 视觉空间推理 / POPE 视觉幻觉评估 / 多图与视频理解） | **本期项目仅覆盖纯语音链路，VLM 不在范围**；多模态前端中的"视觉"仅指消费上游视觉模块已提供的人脸/骨骼坐标用于声视联合定向，不做视觉内容理解；供应商不得将 VLM 能力纳入投标范围与报价 |

**依赖条件**

| 条件 | 是否必需 | 缺失处理 |
|------|----------|----------|
| 目标模块名称 | 必需 | 触发追问，列出七大模块选项 |
| 业务场景说明 | 可选 | 默认墨甲机器人家庭/商业部署场景 |
| 文档版本号 | 可选 | 默认 v1.0 |

---

## 2. 核心需求（Core Requirement）

用户指定七大语音模块之一，本技能自动输出该模块完整的标准化产品需求文档。文档须覆盖功能定义、量化性能指标、上下游接口规范、异常处理策略、可执行验收标准，无需用户二次补充。具体指标要求如下。

### 2.0 测试工况统一约定（参考海外公版端到端响应规范）

> 本 SOR 所有性能指标若未单独标注场景，默认按以下统一工况测试。供应商不得擅自更换更宽松条件复测。

| 项目 | 设定值 |
|------|--------|
| 机器人本体状态 | 静止站立（关节微动作可接受） |
| 麦克风处环境噪声 | 45-50 dBA |
| 麦克风处语音声压 | 65-75 dBA |
| 说话人距离 | 1 m（基线） |
| 语速 | 220-300 词/min |
| 语言 | 中文（一期）；中英文混读单独验收 |
| 内存占用率 | 60%-80% |
| **VAD pause time** | 300 ms（国内项目设定；端到端响应正常比海外 500 ms 设置多约 200 ms） |

**运动场景补充工况**（仅 §2.3 §2.4 中标"运动状态"行使用）：
机器人步行 ≤ 1 m/s，关节电机噪声叠加在 45-50 dBA 环境底噪上；其他参数同上。

### 2.1 纯声学前端信号处理——核心指标基线

| 子功能 | 核心指标 | 目标值 | 测试条件 |
|--------|----------|--------|----------|
| 回声消除（AEC） | 回声抑制量（ERLE） | ≥ 30 dB | TTS播放90dBSPL，1m距离，无运动 |
| 回声消除（AEC） | 残余回声电平 | ≤ -40 dBFS | 同上 |
| 噪声抑制（NS） | SNR改善量 | ≥ 10 dB | 工厂噪声底噪65dBSPL叠加人声 |
| 噪声抑制（NS） | 语音失真度（DMOS） | ≤ 0.5 分 | MOS对比参考语音，5分制 |
| 噪声抑制（NS） | 结构噪声抑制（电机/关节） | ≥ 15 dB | 墨甲电机全速运转，麦克风距电机30cm（评测基准距离；实机部署按实际安装距离复测，按6dB/倍距折算） |
| 增益控制（AGC） | 输出电平 | -26 dBFS ± 3 dB | 30cm~3m范围内说话人，正常音量 |
| 波束成形（默认硬件） | 主瓣增益 | ≥ 6 dB（**6阵元圆阵**，与§3.2默认硬件对齐） | 目标方向0°，旁瓣90°，远场弥散噪声 |
| 波束成形（降级硬件） | 主瓣增益 | ≥ 4 dB（**4阵元线阵**，4元理论上限6dB，工程值留余量） | 目标方向0°，旁瓣90°，远场弥散噪声 |
| 整体处理时延 | 算法延迟 | ≤ 30 ms | 16kHz单帧处理，ARM Cortex-A78 |

### 2.2 多模态前端信号处理——核心指标基线

| 子功能 | 核心指标 | 目标值 | 测试条件 |
|--------|----------|--------|----------|
| 声源定向（DOA） | 方位角误差 | ≤ 15°（P90） | 水平±90°范围，1~3m，安静环境 |
| 声视融合锁定 | 说话人正确锁定率 | ≥ 92% | 单说话人，1~3m，正面180° |
| 多人仲裁 | 目标说话人正确选择率 | ≥ 88% | 2人同时发言，距离差≥0.5m |
| 声视时间对齐误差 | 时间戳对齐精度 | ≤ 40 ms | 视觉30fps，声学16kHz，纯软件时间戳同步（1帧周期同量级） |
| 跟踪切换延迟 | 说话人移动后重新锁定时间 | ≤ 200 ms | 说话人以1m/s横向移动 |

### 2.3 唤醒——核心指标基线

| 场景 | 指标 | 目标值 | 测试条件 |
|------|------|--------|----------|
| 安静近场 | 唤醒率（WRR） | ≥ 97% | 1m，0°正面，SNR ≥ 25dB，100次测试 |
| 噪声远场 | 唤醒率（WRR） | ≥ 90% | 3m，±45°，SNR ≥ 5dB，100次测试 |
| 机器人运动中 | 唤醒率（WRR） | ≥ 85% | 步行状态，1m，SNR ≥ 10dB，50次测试 |
| 全天候 | 误唤醒率（FAR） | ≤ 1 次/8h | 典型家庭环境（电视/音乐背景声）持续播放 |
| 端侧部署 | 模型大小 | ≤ 10 MB | 端侧推理，不含语言模型 |
| 功耗 | 唤醒模块待机功耗 | ≤ 0.5 W | NPU独立运行，其余模块休眠 |
| 响应速度（统一工况） | 唤醒响应时间（说完唤醒词→唤醒事件触发） | ≤ 350 ms | **对齐海外公版** §2.0 统一工况，含系统播放器延迟 ≤ 50 ms |
| 响应速度（算法纯延迟，内部预算） | 唤醒词检测延迟（不含播放器） | ≤ 50 ms | 仅算法侧，从唤醒词结束到模型置信度输出 |

### 2.4 识别（ASR）——核心指标基线

| 场景 | 指标 | 目标值 | 测试条件 |
|------|------|--------|----------|
| 安静环境 | 字错误率（CER） | ≤ 5% | 标准500句测试集，SNR ≥ 25dB |
| 噪声环境 | 字错误率（CER） | ≤ 10% | 同测试集，SNR ≥ 5dB，家庭背景噪声 |
| 运动状态 | 字错误率（CER） | ≤ 15% | 墨甲步行，电机噪声叠加，1m距离 |
| 流式首字上屏延迟（在线） | 说话开始→ASR 首字输出 | ≤ 900 ms | **对齐海外公版**，§2.0 统一工况，在线含网络传输耗时 |
| 流式首字上屏延迟（离线） | 说话开始→ASR 首字输出 | ≤ 850 ms | **对齐海外公版**，§2.0 统一工况，离线本地模型；小语种比中文多 ~200 ms |
| 出文延迟（运动状态，机器人特性必保留） | VAD句末触发→最终结果输出 | ≤ 450 ms | 机器人步行 ≤ 1 m/s，关节电机噪声叠加，1m 距离 |
| 热词召回 | 热词识别率 | ≥ 95% | 100条注入热词，每词测试10次 |
| 标点预测 | 句级标点准确率 | ≥ 90% | 200句测试，人工标注对比 |
| 端点检测 | 误切断率 | ≤ 3% | 停顿≤800ms不断句，200句含停顿语料 |

### 2.5 中枢系统——核心指标基线

| 功能 | 指标 | 目标值 | 测试条件 |
|------|------|--------|----------|
| 意图识别 | 五类意图分类准确率 | ≥ 90% | 500条标注测试集（运动/查询/任务/闲聊/无效各100条） |
| 槽位填充 | 实体提取准确率 | ≥ 88% | 同测试集，动作/对象/位置/时间四类槽位 |
| 多轮对话 | 上下文指代消解准确率 | ≥ 85% | 100组多轮对话（≤5轮），含代词/省略 |
| 指令路由 | 规则引擎路由准确率 | ≥ 98% | 200条明确指令（无歧义），规则覆盖范围内 |
| 路由分流（稳态） | 简单指令规则路由覆盖率 | ≥ 80% | 200条明确简单指令稳态分流统计；剩余≤20%进入LLM路径 |
| 降级策略（故障态） | LLM不可用时规则兜底覆盖率 | ≥ 80% | Top 100高频指令集，断网状态测试；与上一行为不同业务态 |
| 处理延迟 | 意图解析+路由下发延迟 | ≤ 100 ms | 文本输入，规则引擎路径 |
| 处理延迟 | LLM路径总延迟（云端） | ≤ 3000 ms（P99） | 含网络RTT≤150ms+云端LLM推理；指令长度≤50字 |
| 安全拦截 | 危险动作指令拦截率 | 100% | 50条预设危险指令测试集（含高速/攻击性动作） |

### 2.6 TTS——核心指标基线

| 功能 | 指标 | 目标值 | 测试条件 |
|------|------|--------|----------|
| 自然度（核心，shall） | 主观音质评分（MOS） | ≥ 4.1 | 盲测，5分制，≥20名听测人，100句测试语料；对齐讯飞/思必驰主流 TTS 实测 4.1-4.3 区间 |
| 自然度（体验，should） | 主观音质评分（MOS） | ≥ 4.3 | 同测试集；列为加分项，对齐讯飞 SuperVoice / 字节火山高保真版水准 |
| 流式首帧 | 首音频帧输出延迟（TTFT） | ≤ 200 ms | 句长≤20字，云端合成，**不含网络RTT**（网络RTT单独预算见§6） |
| 完整句延迟 | 20字句完整输出时长 | ≤ 500 ms | 同上，不含网络RTT |
| 发音准确率（核心） | 多音字/专有名词正确率 | ≥ 95% | 200句含多音字和墨甲专有名词测试集（含定制词典） |
| 发音准确率（体验） | 多音字/专有名词正确率 | ≥ 98% | 同测试集；列为体验指标，作为长期优化目标 |
| 中英混读 | 英文单词/缩写发音正确率 | ≥ 95% | 100句含英文的测试语料 |
| 情感适配 | 情感标签正确体现率 | ≥ 85% | 询问/确认/警告/闲聊四类，各25句，盲测打分 |
| 嘴型同步 | 字级时间戳误差 | ≤ 40 ms | 与视觉嘴型驱动模块联调，逐字对比 |

### 2.7 大模型功能——核心指标基线

> **范围声明**：本模块仅含纯文本 LLM 推理、RAG、Function Call、上下文管理；**不含任何视觉语言（VLM）能力**。供应商投标本模块时不得绑定 VLM 报价。

#### 2.7.1 业务能力指标（产品集成层）

| 功能 | 指标 | shall（核心） | should（体验） | 测试条件 |
|------|------|--------------|---------------|----------|
| Function Call | 动作映射正确率 | ≥ 85% | ≥ 92% | 30条复杂多轮指令测试集，含 moveArm/navigate/graspObject |
| Function Call | 危险指令拦截率 | 100% | 100% | 50条预设危险指令，安全层过滤前测试；shall 与 should 一致，安全不分层 |
| RAG检索 | 知识召回相关度（P@3） | ≥ 80% | ≥ 88% | 100条墨甲知识库查询，人工标注相关文档对比 |
| 推理延迟 | 云端LLM P95延迟 | ≤ 2000 ms | ≤ 1500 ms | 指令长度≤50字，含网络RTT≤150ms |
| 推理延迟 | 云端LLM P99延迟 | ≤ 3000 ms | ≤ 2200 ms | 同上 |
| 本地降级 | 断网时高频指令覆盖率 | ≥ 80% | ≥ 90% | Top 100指令集，本地7B量化模型 |
| 上下文管理 | 4轮对话后意图连贯性 | ≥ 88% | ≥ 92% | 100组多轮测试，人工评分 |
| 成本控制 | 简单指令规则路由覆盖率 | ≥ 80% | ≥ 90% | 200条明确简单指令，与§2.5"稳态分流"指标一致 |

> **关于 MOS（已迁移至 §2.6）**：TTS 主观音质评分 shall ≥ 4.1 / should ≥ 4.3，详见 §2.6。

> **关于 LLM 通用能力基准（MMMLU / CMMLU / GSM8K / BBH / TruthfulQA / CoT）**：第一版（0→1）不纳入本 SOR 验收；二期再补，用于供应商资格筛查与模型版本变更回归。

---

## 3. 输入规范（Input Spec）

### 3.1 必填输入

| 输入项 | 有效值 | 格式要求 | 缺失时处理 |
|--------|--------|----------|------------|
| 目标模块名称 | 纯声学前端 / 多模态前端 / 唤醒 / 识别 / 中枢系统 / TTS / 大模型功能 | 自然语言，支持模糊映射 | 停止执行，输出七大模块选项列表，主动追问 |

### 3.2 可选输入

| 输入项 | 说明 | 缺失默认值 |
|--------|------|------------|
| 部署场景 | 家庭/商业服务/工业协作 | 家庭+商业混合部署 |
| 硬件配置 | 麦克风阵列型号/数量 | 6阵元圆形阵列，48kHz |
| 文档版本号 | PRD版本 | v1.0 |
| 特殊约束 | 用户追加的墨甲专属限制 | 按通用指标基线处理 |

### 3.3 无效输入判定与处理

| 无效类型 | 判定标准 | 处理方式 |
|----------|----------|----------|
| 空白输入 | 无任何模块关键词，输入字数 < 5字 | 停止执行，输出固定追问话术，不生成任何PRD |
| 模块名模糊 | 含关联词但无法精确匹配（如"降噪"→声学前端） | 自动映射最近似模块，文档头部标注"[已映射为：XXX，如有误请纠正]" |
| 超出七大模块范围 | 含硬件/驱动/算法训练/竞品等关键词 | 告知边界，拒绝执行，提示有效模块范围 |
| 多模块同时请求 | 含≥2个模块名称且含"同时/全部/一起" | 拒绝批量执行，告知每次仅处理一个，询问优先处理哪个 |

---

## 4. 执行工作流规范（Workflow Spec）

| 步骤编号 | 步骤名称 | 输入 | 强制执行条件 | 输出 | 最大执行时长 |
|----------|----------|------|--------------|------|--------------|
| Step 1 | 输入校验 | 用户原始输入 | 任何情况下首先执行，失败则终止后续步骤 | 确认模块名称 / 追问话术 | 立即 |
| Step 2 | 模块基线加载 | 确认的模块名称 | Step 1通过后必须执行 | 该模块指标基线（内部） | 立即 |
| Step 3 | 功能定义撰写 | 模块基线 | 不可跳过，每条含输入/处理逻辑/输出三要素 | 功能定义表格 | — |
| Step 4 | 性能指标量化 | 模块基线 + 第2章对应指标 | 不可跳过，必须填入具体数值，禁止留空或填"待定" | 三级指标表格（核心/体验/监控） | — |
| Step 5 | 接口规范 | 模块定位中的上下游模块 | 不可跳过，上下游双向接口各自独立，字段完整 | 接口规范表格 | — |
| Step 6 | 异常处理 | 模块基线 + 通用异常库 | 不可跳过，覆盖≥5类，含触发条件量化值 | 异常处理表格 | — |
| Step 7 | 验收标准 | Step 3~6产出物 | 不可跳过，每条功能/指标对应一条可执行测试用例 | 验收标准表格 | — |
| Step 8 | 格式自检 | 全文草稿 | 不可跳过，修正后再输出，不向用户暴露自检过程 | 最终PRD文档 | — |

**强制顺序**：Step 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8，不可倒置，不可合并，不可省略。

---

## 5. 输出交付规范（Output Spec）

### 5.1 文档结构

| 章节编号 | 章节名称 | 内容要求 | 禁止项 |
|----------|----------|----------|--------|
| 文档头部 | 模块/版本/日期/作者 | 四字段必须出现，缺项留空但标签保留 | 省略任意字段标签 |
| 一 | 功能定义 | 所有子功能，每条含输入/处理逻辑/输出，表格形式 | 纯文字段落叙述，缺少三要素任一项 |
| 二 | 性能指标 | 核心指标/体验指标/监控指标三级，全部量化，含测试条件 | 无测试条件的裸数字，"较快/较准"等主观词 |
| 三 | 上下游接口 | 上游输入接口和下游输出接口各自独立，含数据类型/格式/时延SLA/异常码 | 缺失任意接口字段，上下游合并描述 |
| 四 | 异常处理 | ≥5类异常，每类含触发条件（量化）/检测方式/处理动作/恢复条件 | 触发条件不量化，"视情况处理"等模糊表述 |
| 五 | 验收标准 | 每条指标对应一条测试用例，含测试方法/通过条件/测试工具 | 无测试工具，无具体通过条件，纯文字描述 |

### 5.2 性能指标格式规范

所有指标必须严格遵循以下格式，无例外：

```
[指标名称] ≥/≤ [数值] [单位]（[测试场景]，[测试工具/方法]）
```

**合格示例：**
- 字错误率（CER）≤ 5%（500句标准测试集，SNR ≥ 25dB，AISHELL-2评测脚本）
- 唤醒率（WRR）≥ 97%（1m，0°正面，安静环境，100次重复测试）
- TTS首帧延迟 ≤ 200ms（句长≤20字，云端合成，Wireshark抓包计时）

**不合格示例（禁止出现）：**
- ~~识别准确率较高~~ → 无数值，不合格
- ~~延迟≤200ms~~ → 无测试条件，不合格
- ~~唤醒效果好~~ → 主观描述，不合格

### 5.3 验收标准格式规范

每条验收标准必须包含四个字段：

| 字段 | 要求 | 示例 |
|------|------|------|
| 验收项 | 对应功能点或指标名称 | 唤醒率（WRR）安静近场 |
| 测试方法 | 可执行步骤，含测试集/工具/环境 | 在消音室1m处朗读唤醒词100次，记录成功次数 |
| 通过条件 | 量化的pass/fail判定 | 成功次数 ≥ 97次 |
| 测试工具 | 具体工具名称或脚本 | 定制化语音评测脚本 + 声级计校准 |

### 5.4 字数与篇幅

| 文档类型 | 字数范围 | 表格行数下限 |
|----------|----------|--------------|
| 单模块PRD | 1500~3000字（含表格内容） | 功能定义≥5行，性能指标≥8行，异常处理≥5行，验收标准≥8行 |

---

## 6. 全局约束与风控（Constraints）

| 编号 | 约束类型 | 约束内容 | 违反后处理 |
|------|----------|----------|------------|
| C-01 | 范围约束 | 仅处理七大模块范围内的PRD撰写，不跨界到硬件/驱动/算法训练 | 告知边界，拒绝执行超范围部分 |
| C-02 | 数量约束 | 每次执行仅生成一个模块的PRD | 多模块请求时告知限制，询问优先模块 |
| C-03 | 指标约束 | 所有性能指标必须量化，包含数值+单位+测试条件，三者缺一不合格 | 无法量化时填"[待测试集确定，参考建议：XXX]"，不留空，不填"待定" |
| C-04 | 结构约束 | 五章节固定，顺序锁死，任何情况不增删章节、不调整顺序 | 自检阶段强制修正 |
| C-05 | 内容约束 | 禁止主观形容词（快/准/好/优秀/流畅），禁止未经标注的行业经验值 | 自检阶段强制替换为量化表述 |
| C-06 | 安全约束 | 中枢系统和大模型功能模块中，危险动作拦截必须单独列为一条验收标准，通过条件为100%拦截率 | 缺失则自检补入 |
| C-07 | 一致性约束 | 同一指标在功能定义、性能指标、验收标准三章中数值必须一致 | 自检阶段对齐，不一致则以性能指标章节为准 |
| C-08 | 时延预算约束 | 端到端总预算分两条独立通道：**规则路径**安静≤800ms / 噪声≤1200ms；**LLM路径**安静≤3500ms / 噪声≤4000ms（P95，含首字反馈"让我想一下"在内的整体响应） | 生成每个模块时标注该模块在规则/LLM路径中的时延预算占比 |

**端到端时延预算分配表（强制约束）**

**A. 规则路径（简单确定性指令，覆盖率 ≥ 80%）**

| 模块 | 时延预算（安静） | 时延预算（噪声） |
|------|----------------|----------------|
| 声学前端 | ≤ 30 ms | ≤ 30 ms |
| 多模态前端 | ≤ 50 ms | ≤ 50 ms |
| 唤醒 | ≤ 50 ms | ≤ 80 ms |
| 识别（ASR）出文延迟 | ≤ 300 ms | ≤ 450 ms |
| 中枢系统（规则路径） | ≤ 100 ms | ≤ 100 ms |
| TTS首帧（不含云端RTT） | ≤ 200 ms | ≤ 200 ms |
| 云端网络RTT（TTS云端时） | ≤ 150 ms | ≤ 150 ms |
| **合计** | **≤ 880 ms（目标≤800ms，含余量预警）** | **≤ 1060 ms（目标≤1200ms）** |

**B. LLM路径（复杂/模糊指令，覆盖率 ≤ 20%）**

| 模块 | 时延预算（安静） | 时延预算（噪声） |
|------|----------------|----------------|
| 声学前端 | ≤ 30 ms | ≤ 30 ms |
| 多模态前端 | ≤ 50 ms | ≤ 50 ms |
| 唤醒 | ≤ 50 ms | ≤ 80 ms |
| 识别（ASR）出文延迟 | ≤ 300 ms | ≤ 450 ms |
| 中枢系统（LLM路径路由+回填） | ≤ 100 ms | ≤ 100 ms |
| 云端LLM推理（含网络RTT，P95） | ≤ 2000 ms | ≤ 2500 ms |
| TTS首帧（不含云端RTT） | ≤ 200 ms | ≤ 200 ms |
| 云端网络RTT（TTS云端时） | ≤ 150 ms | ≤ 150 ms |
| **合计（P95）** | **≤ 2880 ms（目标≤3500ms）** | **≤ 3560 ms（目标≤4000ms）** |

> **首字反馈策略**：LLM路径下，规则引擎应在 ≤ 800ms（安静）/ ≤ 1200ms（噪声）内先返回首字反馈（如"让我想一下"），LLM真实结果到达后再继续语音输出，避免用户感知到 3 秒级的"卡顿"。

---

### 6.5 端到端业务域响应表（对外承诺，参考海外公版基线）

> 业务域分类来源于海外公版端到端响应规范（车端：车控/电话/地图/多媒体/天气/首字上屏），映射至机器人场景。  
> 测试工况：§2.0 统一工况  
> 计时口径：用户说完最后一字 → 机器人首字 TTS 输出 / 内部 API 调用下发

#### A. 在线响应（联网状态）

| 业务域 | 机器人对应场景 | shall | should | 海外公版对标 |
|--------|--------------|-------|--------|-------------|
| **唤醒响应** | 唤醒词→唤醒事件 | ≤ 350 ms | ≤ 300 ms | 海外 ≤ 350 ms |
| **本体控制**（机器人控制） | 移动/转身/抓取/手势/姿态 | ≤ 1500 ms | ≤ 1300 ms | 车控 1500（极氪 1500 / 领克 1300） |
| **信息查询** | 时间/状态/简单本地知识 | ≤ 2200 ms | ≤ 1500 ms | 天气 2500（极氪 1500 / 领克 2000） |
| **多媒体** | 音乐/视频/播放控制 | ≤ 2200 ms | ≤ 2200 ms | 多媒体 2200（极氪 2200 / 领克 2300） |
| **任务执行** | 多步骤组合任务（含云端 API 链） | ≤ 2500 ms | ≤ 2100 ms | 地图 2500（极氪 2100 / 领克 2300） |
| **对话闲聊**（LLM 路径，P95） | 开放域闲聊 / 复杂语义理解 | ≤ 3000 ms | ≤ 2200 ms | 电话 1700 + LLM 推理 ~1300（机器人项目特性） |
| **ASR 首字上屏** | 说话开始→ASR 首字 | ≤ 900 ms | ≤ 700 ms | 海外 900 / 一汽 700 |

#### B. 离线响应（断网兜底）

| 业务域 | shall | should | 海外公版对标 |
|--------|-------|--------|-------------|
| **本体控制** | ≤ 1400 ms | ≤ 1200 ms | 车控 1400 |
| **信息查询**（本地 KB） | ≤ 1500 ms | ≤ 1300 ms | 电话 1500 |
| **ASR 首字上屏** | ≤ 850 ms | ≤ 650 ms | 海外 850 / 极氪 700 / 领克 650 / 一汽 650 |
| 多媒体 / 任务执行 / 对话闲聊 | 一期**不强制**离线 | — | — |

#### C. 端到端任务成功率

| 任务类型 | shall | should | 测试方法 |
|----------|-------|--------|----------|
| 本体控制端到端成功 | ≥ 92% | ≥ 96% | ASR 正确 ∧ NLU 正确 ∧ Function Call 正确 ∧ API 下发成功 |
| 信息查询端到端成功 | ≥ 90% | ≥ 95% | 同，含信息源返回成功 |
| 复杂多轮（≥ 3 轮）任务 | ≥ 80% | ≥ 88% | 100 组多轮测试 |
| 运动状态全任务成功率 | ≥ 80% | ≥ 88% | 同测试集，机器人步行中 |

#### D. 网络切换无感

| 指标 | shall | should |
|------|-------|--------|
| 4G 信号丢失 → 本地切换 | ≤ 500 ms | ≤ 200 ms |
| 切换瞬间指令丢失 | 0 | 0 |
| 本地 → 云端恢复 | ≤ 1 s | ≤ 500 ms |

---

## 7. 异常兜底规范（Exception Spec）

### 7.1 Skill执行层异常

| 异常类型 | 触发条件 | 检测方式 | 唯一处理逻辑 | 恢复条件 |
|----------|----------|----------|--------------|----------|
| 空白输入 | 输入字数 < 5字，无七大模块关键词 | 输入长度检测 + 关键词匹配 | 输出标准追问话术，列出七大模块选项，停止执行，不生成任何内容 | 用户重新输入有效模块名称 |
| 模块名模糊 | 含关联词但无法精确匹配（如"语音输入"） | 模糊语义匹配 | 自动映射最近似模块，文档头部标注"[已映射为：XXX，如有误请纠正]"，继续执行 | 用户确认或纠正映射 |
| 超出范围 | 含硬件/驱动/竞品/算法训练等禁用关键词 | 关键词黑名单匹配 | 告知边界"本技能聚焦墨甲语音软件链路七大模块PRD撰写，[具体超范围内容]不在范围内"，停止执行 | 用户重新输入目标模块 |
| 多模块同时请求 | 含≥2个模块名称且带"同时/全部/一起/都" | 多模块关键词检测 | 告知"每次仅处理一个模块，请告知优先处理哪个"，提供排序选项 | 用户指定单一优先模块 |
| 连续2轮追问无效 | 用户对追问连续2次回应中无有效模块信息 | 追问计数器 | 输出七大模块概览表（含每模块一句功能简介），引导用户从表中选择 | 用户点选具体模块 |

### 7.2 内容生成层异常

| 异常类型 | 触发条件 | 检测方式 | 唯一处理逻辑 |
|----------|----------|----------|--------------|
| 指标无法量化 | 特殊部署场景无行业公开基准数据 | 指标库匹配失败 | 填写"[待实测确定；参考行业基线：XXX（行业经验值）]"，不留空，不填"待定" |
| 接口字段不完整 | 自检发现接口缺少数据类型/格式/时延SLA/异常码任意字段 | 自检步骤字段校验 | 自动补全缺失字段（按模块基线标准填入），不向用户暴露自检过程 |
| 验收标准缺测试工具 | 验收条目无具体工具/脚本名称 | 自检步骤字段校验 | 自动补充"定制化语音评测脚本"或对应行业工具，不留空 |
| 时延预算超标 | 某模块时延指标超过第6章分配预算 | 与预算表对比 | 在该指标下方自动添加"⚠️ 注意：当前值超出本模块时延预算（X ms），需与相邻模块协商调整" |
| 危险指令验收缺失 | 中枢/大模型模块验收标准中无危险指令拦截条目 | 模块名称+验收章节关键词检测 | 自动在验收标准章节末尾补入：危险指令拦截率，测试方法：50条预设危险指令，通过条件：100%拦截 |

---

## 8. 验收标准（Acceptance Criteria）

### 8.1 触发精准性验收

| 验收项 | 测试方法 | 通过条件 | 测试工具 |
|--------|----------|----------|----------|
| 正确触发率 | 输入含七大模块名称+撰写类动词的50条指令，记录触发成功次数 | 50次全部正确触发，触发率100% | 手动测试，记录触发日志 |
| 误触发率 | 输入20条非PRD撰写类请求（诊断/分析/对比），记录误触发次数 | 误触发次数 = 0 | 手动测试 |
| 优先级冲突 | 同时激活多个技能，验证priority=6是否生效 | 本技能优先级正确生效，无被覆盖 | Claude Code技能冲突测试 |

### 8.2 流程闭环验收

| 验收项 | 测试方法 | 通过条件 | 测试工具 |
|--------|----------|----------|----------|
| Step顺序正确 | 对7个模块各触发一次，检查输出章节顺序 | 7次均为一→二→三→四→五，无乱序 | 人工审查输出文档 |
| 步骤不可跳过 | 检查空白输入时是否存在PRD内容生成 | 空白输入时0条PRD内容，仅追问话术 | 手动测试 |
| 自检生效 | 故意提供缺失测试工具的指标，检查输出是否自动补全 | 输出文档中无空白测试工具字段 | 人工审查输出文档 |

### 8.3 输出质量验收

| 验收项 | 测试方法 | 通过条件 | 测试工具 |
|--------|----------|----------|----------|
| 指标量化率 | 随机抽取3个模块的PRD，统计性能指标章节中包含数值+单位+测试条件的条目占比 | ≥ 95%的指标条目格式合规 | 人工逐条审查 |
| 功能定义三要素 | 统计功能定义表格中包含输入/处理逻辑/输出三要素的行数占比 | 100%的功能条目包含三要素 | 人工逐行审查 |
| 接口字段完整性 | 检查接口规范章节的数据类型/格式/时延SLA/异常码四字段 | 100%的接口条目四字段完整 | 人工逐行审查 |
| 异常处理覆盖数 | 统计异常处理章节的异常类型总数 | 每个模块≥5类异常 | 人工统计 |
| 验收标准可执行性 | 检查验收标准中是否每条含测试方法/通过条件/测试工具 | 100%的验收条目三字段完整 | 人工逐行审查 |
| 时延预算合规 | 对比各模块输出的时延指标与第6章预算表 | 无模块时延指标超过预算表限值（或有⚠️标注） | 与预算表逐项对比 |
| 危险指令验收必存在 | 检查中枢系统和大模型功能模块的验收标准 | 两模块均含危险指令拦截率≥100%的验收条目 | 人工审查 |

### 8.4 异常兜底验收

| 验收项 | 测试方法 | 通过条件 | 测试工具 |
|--------|----------|----------|----------|
| 空白输入处理 | 发送5次空白输入，检查是否有PRD内容生成 | 5次全部输出追问话术，0次生成PRD内容 | 手动测试 |
| 多模块请求处理 | 发送"同时生成唤醒和ASR模块PRD"，检查输出 | 输出拒绝说明+优先模块询问，无批量PRD内容 | 手动测试 |
| 超范围请求处理 | 发送"帮我选麦克风阵列型号"，检查输出 | 输出边界说明，无PRD内容，无超范围建议 | 手动测试 |
| 无法量化指标处理 | 提供无行业基准的小众场景，检查指标字段 | 指标字段填"[待实测确定；参考建议：XXX]"，无空白，无"待定" | 人工审查输出 |

### 8.5 内容合规验收

| 验收项 | 通过条件 |
|--------|----------|
| 主观形容词 | 全文0个"快/准/好/优秀/流畅/较高"等主观词汇 |
| 裸数字 | 全文0个无单位无条件的裸数字指标 |
| 虚构数据 | 全文0条未标注来源且超出第2章基线范围的数字 |
| 模块边界混淆 | 全文0处将某模块职责混入相邻模块的描述 |
| 章节结构 | 全文5章节完整，顺序正确，无缺失章节 |

---

*SOR v1.0 | 与 Voice_AI_Signal_Expert_SKILL.md v1.0 配套使用 | 如产品指标更新，本SOR第2章需同步修订*
